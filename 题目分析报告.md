# E 题第二问：局部模态缺失下的情感预测建模方案

> 版本：2026-09-23，建模设计稿。本稿只规划第二问，不包含训练结果；参赛队需独立核对、实现、实验并决定最终论文表述。第一问的自制特征不进入第二问、第三问的训练或调参。

## 1. 题目、附件与结论目标

原题为 [《复杂场景下多模态情感识别的数学建模与算法设计》](../复杂场景下多模态情感识别的数学建模与算法设计.docx)。第二问要求在文本、语音、视觉的**局部连续时段**缺失时，同时预测情感极性和强度，分析缺失模态类型、位置、时长的影响，并对附件三无标签样本输出预测。题面明确：第二、三问仅用附件二 **train** 学习参数，用 **valid** 选择结构、超参数和阈值；附件三、四仅作对应专项最终推理；极性用 Accuracy、F1，强度用 MAE、Pearson 评价。全题仅可使用赛题给出的 CMU-MOSEI 系列数据；允许开源预训练模型和框架，须注明版本与用途。

| 附件 | 本阶段作用 | 边界 |
|---|---|---|
| 附件一 100 个原始视频与标签 | 第一问处理与审查 | 不进入第二问训练、标准化、阈值选择 |
| 附件二 `aligned_50.pkl` | 第二问训练、验证；封存的 test 可在模型冻结后作一次独立核验 | 保留 train/valid/test 原划分和样本 ID |
| 附件三对齐版 30 个无标签样本 | 最终推理与预测 CSV | 不参与训练、调参、标签统计或模型选择 |
| 附件四对齐版 20 个样本 | 第三问专项 | 本阶段不使用 |

第二问最终需要给出：①每条附件三样本的极性与强度；②在验证集不同缺失条件下的 Accuracy、F1、MAE、Pearson；③缺失类型、位置、长度与性能变化的规律及不确定性；④与不做缺失增强的对照；⑤可复现的掩码生成、模型参数和输入版本。这里没有预写任何性能提升结论。

## 2. 已核对的数据事实与风险

工程读取器与审计记录显示：附件二对齐版有 train 3395、valid 728、test 727 条；文本 `text` 为 $50\times768$、语音 `audio` 为 $50\times74$、视觉 `vision` 为 $50\times35$，`text_bert` 为 $3\times50$（token ID、有效位掩码、type ID）。50 是**最大 token 序列位置数**，不是把视频等分成 50 个等长秒段；不同样本有效长度不同，附件二也没有逐词真实时间戳或原始波形、图像。极性标签为负/中/正三类，强度在 $[-3,3]$。train 类别数为 967/758/1670，须报告 macro-F1 和逐类结果。字段与实际形状依据 `results/reader_check.json`、`results/interface_contract.json` 以及只读附件二哈希 `66e867aa74bc70a844e806e5571e371c9abb4a35f9e2887ce9b4d97ff2cb8fcd`。

`text_bert` 的有效位要与 `[CLS]`、`[SEP]` 区分。train 中除这两个特殊位外，语音有 65 个、视觉有 4249 个有效位置全零向量；这些只能记为**疑似无观测**，不能从零值推断真实遮挡原因（`results/mask_audit.json`）。附件二原始语音、视觉为 float64，训练输入转 float32；任何 NaN/Inf 使训练停止。标准化参数只能从 train 的有效、可观测值估计。掩码先从原始特征计算，再标准化，并将缺失位及补齐位重新置零，避免零值被均值变换成非零“伪观测”。文本可用投影层后 LayerNorm；语音和视觉按训练集每维标准化。

附件三对齐版不提供 `text(50,768)`，提供 `text_bert(3,50)`、`audio`、`vision`。工程已固定 `bert-base-uncased` revision `86b5e0934494bd15c9632b12f734a8a67f723594`；已有全量重编码审计在附件二与附件四 4870 条上的最大绝对差为 $5.966\times10^{-4}$，0 条超过 $10^{-3}$（`results/bert_audit_full.json`）。因此附件三推理时用**冻结、同版本** BERT 从 token ID 重建文本特征，不能填造一个官方未提供的文本向量。附件三仅作接口检查时，30 个文件的有效内部 token ID 没有 0；其语音、视觉存在内部全零向量。此事实只用于确认读取方式，不用于设定增强比例或模型参数。原始附件在本轮读取前后完整性核验均 PASS，各检查 245 个文件。

已有训练 demo 是 38.55 万参数的完整数据速度测试，RTX 5070 Ti 16 GB 上 20 轮训练及验证约 8.09 秒，最优 valid MAE 0.6294。它没有实现本题所需的局部缺失增强，因此**不是第二问的鲁棒性基线或最终成绩**；新网络的耗时必须在实际运行后测量（`E题/训练demo/results/report.json`）。

## 3. 八项模型合同

| 合同项 | 第二问的约定 |
|---|---|
| 任务定义 | 基于 $50$ 个 token 对齐位置上的三模态特征，预测每段视频的三类情感极性 $c_i$ 与连续强度 $y_i$；研究局部连续缺失对两任务的影响。 |
| 数据 | 训练样本仅来自附件二 aligned train；验证仅用 aligned valid；附件三对应 aligned 版本，只作最终推理。原始文件只读。 |
| 结论类型 | 极性与强度预测、总体及分场景指标、同样本缺失前后性能差、按缺失类型/位置/长度的曲线或表。 |
| 模型 | 一个简单的掩码感知汇聚基线，加一个主模型“可靠性门控 + 时序编码 + 双任务输出”；两种模型体系以内。 |
| 数学定义 | 使用有效位、原有零向量和模拟缺失三层掩码；损失由三类交叉熵和强度 Huber 损失组成；预测强度限制在 $[-3,3]$。 |
| 实现 | Python、NumPy、PyTorch 和已有冻结 BERT 快照；固定随机种子、数据哈希、标准化统计、掩码种子、模型权重和版本。 |
| 验证 | clean 与固定缺失面板的 valid 指标；在同一模型结构内比较“无增强/有增强”；最后对封存 test 作一次独立核验；附件三只输出无标签预测。 |
| 风险 | 文本上下文泄漏、把 padding 当缺失、零向量含义未知、类别不均衡、模型只会读文本、验证集过拟合、附件三文本接口差异。触发时按第 8 节回退。 |

### 假设及依据

| 级别 | 假设 | 依据和放宽后的复验 |
|---|---|---|
| 关键 | aligned 文件同一序列位置的三模态可作为同一 token 对齐步处理。 | 题面“对齐版”和统一读取器；若字段顺序不一致，停止融合并回到附件接口核验。它不代表真实秒级同步。 |
| 关键 | 人为抹去局部特征后的样本沿用原片段级标签。 | 题面要求研究局部缺失下原情感预测；若长缺失使信息不可辨，用分长度结果展示极限，不伪称标签仍可由剩余数据判定。 |
| 关键 | 附件二 train/valid 的标签和拆分可用于监督/选型，附件三不参与选型。 | 题面第 30 段及 59–63 段；任何跨拆分拟合立即重跑。 |
| 可放宽 | 模态全零向量按“疑似无观测”处理。 | 已有审计只证明全零；复验时比较“全零视缺失”和“全零视正常输入”的验证表现，并保留解释边界。 |
| 可放宽 | 预设连续遮蔽比例覆盖轻、中、重缺失。 | 题面只规定连续局部缺失，不给比例；验证面板按有效 token 数固定分层，报告敏感性，不借附件三调比例。 |

## 4. 基于 aligned 特征的增强：可行，但必须保留语义

令样本 $i$ 的文本有效位为 $V_{it}$，除 `[CLS]`、`[SEP]` 外的内容位为 $C_{it}$；模态 $m\in\{T,A,V\}$ 的原始可观测位记 $O_{itm}$。对音视频，$O_{itm}=C_{it}\mathbf1(\lVert\mathbf x_{itm}\rVert_2>10^{-6})$；对文本，$O_{itT}=C_{it}$。训练时独立生成连续区间遮蔽 $S_{itm}$，最终可用位为 $R_{itm}=O_{itm}(1-S_{itm})$。`padding`、原有全零位置、模拟缺失分别记录，验证时使用固定种子和固定区间。

**推荐增强及可复现抽样**：令内容位数 $K_i=\sum_tC_{it}=L_i-2\geq1$，内容位置按原顺序编号 $0,\ldots,K_i-1$。每个 train 样本保留一个完整视图，再按下面的规则抽一个局部连续遮蔽视图：①单模态概率 $3/4$，此时 T/A/V 等概率；双模态概率 $1/4$，此时 T+A/T+V/A+V 等概率；②长度比例 $r$ 从 $\{0.1,0.3,0.5\}$ 等概率选择，整数长度 $\ell=\min(K_i,\max(1,\lceil rK_i\rceil))$；③起点 $j$ 从整数 $0,\ldots,K_i-\ell$ 等概率抽取，双模态共用同一个 $[j,j+\ell)$ 区间；④使用 `NumPy SeedSequence([训练种子, epoch, train行号])` 派生独立随机流，与 DataLoader 的打乱和 worker 数无关。完整视图与遮蔽视图各产生一次监督损失。若抽出的视图使整段所有内容位均不可用，最多重抽 10 次，仍不满足则退回完整视图。局部所有模态都可同时缺失；若一个模态在短句中被完全遮蔽，按第 5 节的零可用位规则处理。上述比例是**预先规定的实验档位**，并非从附件三反推的真实缺失率。若长文本截断到 50 位，不凭空补出后续词的时间信息。

音频、视觉在有效位置把整行特征置零并记录 $S$，这与题面“特征值全部为零的连续区间”匹配。文本须更谨慎：原 `text` 是看过整句的 BERT 上下文化表示，**只把若干词向量置零会泄漏被遮住词的信息**，因为 `[CLS]` 和未遮住词的向量仍含上下文。文本缺失视图应先把对应 `input_ids` 替换为 `[MASK]`，用冻结 BERT 重编码整句，然后把缺失位置输出向量置零；训练头部不读 `[CLS]`、`[SEP]`。这一做法近似“原词不可见后的上下文”，仍允许从邻词推断，论文须说明其局限。正常 clean 视图可直接使用官方 `text`，附件三完整文本由同一冻结 BERT 重建。任一遮蔽只在特征/冻结编码视图中发生，原 pkl 和原标签永不修改。

不推荐对 74 维语音、35 维视觉做图像翻转、音调变换或随意插值：附件二没有原始波形、图像和逐维物理语义，无法保证这些操作仍对应真实样本。也不把整模态消失作为主训练分布；它可留作额外极限压力测试，和题目的“局部缺失”分开报告。

## 5. 两种模型与训练目标

**模型 A（验证基线）**：对每个模态仅在 $R_{itm}=1$ 的位置做掩码均值汇聚，拼接三个模态汇聚向量及各模态有效比例，经两层宽度 128 的 MLP 输出三类 logits 与强度。令 $n_{im}=\sum_tR_{itm}$；均值分母取 $\max(1,n_{im})$，当 $n_{im}=0$ 时该模态汇聚向量显式设为全零，另附 `has_modality=0` 标志。这样即使样本只有 1 个内容 token、该模态被完全遮蔽，也不会除以零。其用途是检验输入、掩码、标签和指标链路；分别训练“无增强”和“有增强”，看增强本身带来的变化。

**模型 B（主模型）**：各模态分别线性投影到 128 维并做 LayerNorm；在每个有效 token 位置计算三个模态的门控分数，只在 $R_{itm}=1$ 的模态上 softmax，得到融合序列 $\mathbf z_{it}=\sum_m\alpha_{itm}\mathbf h_{itm}$。若该位置三模态都缺失，不计算空集合上的 softmax，而用可学习空位向量与显式存在位表示。再以 1 或 2 层、4 头的轻量 Transformer 编码这段最多 50 位的序列，以内容位掩码汇聚，连接三类 logits 头和强度回归头。强度头用 $3\tanh(\cdot)$ 保证输出在 $[-3,3]$ 且保持梯度。门控权重表示模型内部选择，**不能直接当因果解释**；第三问需要另做可复核解释验证。选择这种轻量结构是基于 3395 条训练样本、对齐后的短序列和局部缺失结构作出的工程推断；不是复现某篇论文的成绩。

记 $x$ 为完整三模态视图、$\tilde x$ 为同一训练样本的连续遮蔽视图。模型输出三维 logits $\mathbf q_i$ 和强度 $\hat y_i$；分类概率为 $\mathbf p_i=\operatorname{softmax}(\mathbf q_i)$，预测类别为 $\hat c_i=\operatorname*{argmax}_k p_{ik}$。$\mathcal L_{\mathrm{CE},w}$ 是以 **logits $\mathbf q_i$** 和真实类别 $c_i$ 计算的三类加权交叉熵，$\mathcal L_{\mathrm{Huber}}$ 是以 $\hat y_i,y_i$ 计算的 Huber 损失。监督目标为

$$
\mathcal L_{\mathrm{sup}}=\mathcal L_{\mathrm{CE},w}(\mathbf q,c)+\lambda_r\mathcal L_{\mathrm{Huber}}(\hat y,y),\qquad
\mathcal L=\mathcal L_{\mathrm{sup}}(x)+\lambda_m\mathcal L_{\mathrm{sup}}(\tilde x).
$$

当前**冻结的第一轮候选空间**：模型 A 的无增强/有增强两版；模型 B 的层数 $\{1,2\}$ 与无增强/有增强交叉，共四版。两种模型均固定投影/隐藏宽度 128、dropout 0.1、Huber 阈值 1、$\lambda_r=\lambda_m=1$、AdamW 学习率 $10^{-3}$、weight decay $10^{-2}$、batch 64、最多 30 轮、patience 5。类别权重 $w_c\propto1/\sqrt{n_c}$ 由 **train** 类频次算出并归一化到三类均值 1；无增强版只计算完整视图监督损失。训练种子固定为 20260923、20260924、20260925。参数属于待运行的建模设定，未宣称最优；若后续扩大搜索空间，应先记录新范围，再重新评价全部候选，不能看过附件三结果后修改。

每轮在同一固定 valid 缺失面板上计算下列选择分数，越低越好：

$$
J=\tfrac14\bigl(\mathrm{MAE}_{\mathrm{clean}}/3+1-F_{1,\mathrm{clean}}\bigr)
 +\tfrac34\bigl(\overline{\mathrm{MAE}}_{\mathrm{mask}}/3+1-\overline{F}_{1,\mathrm{mask}}\bigr).
$$

上横线表示对第 6 节全部 54 个预设缺失场景等权平均；$F_1$ 均为 macro-F1。除以 3 是按情感强度单侧范围进行固定缩放，并非竞赛指定权重。单次训练若连续 5 轮未使 $J$ 至少降低 $10^{-4}$，则恢复历史最小 $J$ 的权重。候选模型以三个种子的最佳 $J$ **均值**排序；若均值差不超过 $10^{-4}$，依次按更低的缺失场景平均 MAE、更少参数、配置名称字典序打破平局。报告所有四项原始指标，不能只展示 $J$。强度与极性双头可能出现符号矛盾，须统计矛盾率；这一轮不加入事后阈值修正或一致性损失。

学术依据：Guo 等在 [ACL 2024](https://aclanthology.org/2024.acl-long.94/) 研究缺失模态下的 Transformer 与缺失提示；He 等在 [CVPR 2026](https://openaccess.thecvf.com/content/CVPR2026/html/He_Enhance-then-Balance_Modality_Collaboration_for_Robust_Multimodal_Sentiment_Analysis_CVPR_2026_paper.html) 研究模态可靠性与不平衡；Zhuang 等在 [ICCV 2025](https://openaccess.thecvf.com/content/ICCV2025/html/Zhuang_CMAD_Correlation-Aware_and_Modalities-Aware_Distillation_for_Multimodal_Sentiment_Analysis_with_ICCV_2025_paper.html) 研究缺失模态下表示不一致。本方案仅借鉴问题意识和可验证方向，结构、增广概率与效果都需在本题数据上实测。

## 6. 验证设计和应交结果

1. 从 train 计算音视频标准化量和类权重。valid 不做随机在线增强，仅保留 clean 视图和下述确定性缺失面板；test 封存。同一结构的基础版本、增强版本使用**同一**缺失面板；A 与 B 因编码器也不同，只作完整模型比较，不将差值解释为门控的单独贡献。记录 PyTorch、Transformers、BERT revision、附件哈希、模型参数量及运行时间。
2. 面板固定缺失模态为 T、A、V、T+A、T+V、A+V 六组，位置为前/中/后，长度比例 $r$ 为 10/30/50%，交叉形成 54 个场景。每条样本沿用第 4 节的 $K_i,\ell$ 规则；内容位起点依次固定为 $j=0,\lfloor(K_i-\ell)/2\rfloor,K_i-\ell$，双模态共用同一连续区间，因此面板无需随机种子。短样本可能使前/中/后取到同一区间，此时位置效应标为不可区分。每个场景给出样本数、实际遮蔽位数、名义区间占内容长度的比例、**新增丢失的原本可观测位比例**（若原本可观测位为 0，则记 NA）、Accuracy、macro-F1、逐类 F1、MAE、Pearson，以及与同一批 clean 样本的差值。报告训练种子的均值与波动；有多个片段同源视频时按 video ID 做簇自助法置信区间，避免把片段当独立视频。
3. 依第 5 节预先固定的 $J$ 和并列规则选择模型，然后比较 clean 与缺失面板的性能差、不同模态/位置/长度的配对差值。若模型在 clean 上明显变差或缺失改善不稳定，不能宣称“鲁棒性提升”。缺失长度的单位是**有效 token 位数及其比例**，不能写成秒。
4. 模型和参数冻结后，可对附件二封存 test **一次**运行同样的预设面板，作为补充独立核验，不再反向调参。附件三 30 条只作最终推理；没有标签，不能在附件三报告 Accuracy/F1/MAE/Pearson。预测文件保留样本 ID、来源文件、极性、强度、三类概率和模型版本；提交列名再与题面第四部分逐项核对。

建议候选图：三模态观测/遮蔽时间格图、各类原始零向量比例图；训练/验证损失与 clean/缺失性能曲线；按缺失模态×长度的指标热图、前中后差值及置信区间图、极性混淆矩阵。图表仅在真实实验后制作，不能拿设计值充当结果。

## 7. 后续实现顺序与停止条件

1. 先冻结数据接口、标签映射、BERT revision、三层掩码和验证面板；以最小 batch 验证张量形状、有限值、padding 不参与汇聚、A3 文本重建输入一致。
2. 跑模型 A 的无增强/有增强完整链路，获得实际基线；再跑模型 B 的无增强/有增强对照。每个模型至少三个种子，保存最优 valid 权重与指标。
3. 若任何 train/valid/test 交叉拟合、源文件哈希改变、模拟遮蔽影响 padding、文本遮蔽仍可读原词、A3 推理依赖未提供字段、或输出范围/格式错误，立即停止并修复。若模型 B clean 与缺失指标均不优于模型 A，保留简单模型并分析原因，不凭复杂度选主模型。
4. 第一问继续独立审查；第三问后续单独设计解释链。此建模稿并不替代第二问代码、实测结果、参赛队人工审查或最终提交。

## 8. 当前证据与来源

- 本地原题 DOCX：题面第 23–30、59–73 段；工程 `results/reader_check.json`、`results/mask_audit.json`、`results/interface_contract.json`、`results/bert_audit_full.json`；速度证据 `E题/训练demo/results/report.json`。
- 官方赛事平台的 [2026 年参赛邀请函](https://cpipc.acge.org.cn/cw/contestNews/detail/4/2c9080189dcfa24e019dddacc24a1314?page=0)：论文 MD5 上传至 2026-09-27 12:00，PDF 上传至 2026-09-28 24:00；本稿是第二问设计，不是可提交论文版本。团队应在正式提交前冻结并人工复核论文与程序。
- 文献页：[Guo et al., ACL 2024](https://aclanthology.org/2024.acl-long.94/)；[Zhuang et al., ICCV 2025](https://openaccess.thecvf.com/content/ICCV2025/html/Zhuang_CMAD_Correlation-Aware_and_Modalities-Aware_Distillation_for_Multimodal_Sentiment_Analysis_with_ICCV_2025_paper.html)；[He et al., CVPR 2026](https://openaccess.thecvf.com/content/CVPR2026/html/He_Enhance-then-Balance_Modality_Collaboration_for_Robust_Multimodal_Sentiment_Analysis_CVPR_2026_paper.html)。双引擎检索脚本本轮已调用，但 OpenAlex/AnySearch 均因本机网络权限拒绝连接；引文改从上述原始出版页面人工核对，未声称双引擎交叉验证。
