# 问题三：可解释性多模态情感预测——论文写作稿

本稿供论文写作者整合、压缩和排版。数字来自 `results/q3_c_uni_final_20260925/` 与 `results/q3_c_uni_validation_full2_20260925/`，日期为 2026-09-25。第三问最终结果**复用第二问已确定的 `C_uni` 三模态模型**，没有选用另行训练的新模型。附件四 20 条没有情感标签，以下绝不将其预测统计写成准确率。

## 一、正文可用稿

### 1. 问题与总体方法

问题三要求在输出情感极性和情感强度的同时，说明模型主要参考了哪些模态，并将局部证据定位回原始文本、语音时段或视觉关键帧。附件数据源于 CMU-MOSEI 多模态情感分析语料[1]。为保持第二、三问的输入和预测口径一致，本文固定第二问在附件二训练集上学习、在验证集上确定的 `C_uni` 参数，构建“**三模态预测器＋模态归因＋局部证据检验＋原视频回溯**”的可解释预测流程。第三问没有新增需要拟合的网络参数；解释模块仅查询固定模型对规定输入扰动的响应。附件四仅用于最终推理和展示，未用于调整模型或解释阈值。

设第 $i$ 个样本的文本、音频、视觉对齐特征分别为 $X_i^T\in\mathbb{R}^{50\times768}$、$X_i^A\in\mathbb{R}^{50\times74}$、$X_i^V\in\mathbb{R}^{50\times35}$。50 为最大序列长度，特殊 token 和填充位置不计入有效内容；音视频特征的全零行作为不可观测候选位置。模型分别将三路序列投影到 96 维，加入位置编码，经独立的一层、四头 Transformer 编码，并在各自有效位置上池化。三个模态表示与各自有效覆盖率拼接后进入 128 维共享层，最终得到三分类概率 $p=(p_-,p_0,p_+)$ 和连续强度 $\hat y=3\tanh(z_r)$，从而保证 $\hat y\in[-3,3]$。极性与强度由两个任务头分别输出，因此需要分别检验其一致性。

`C_uni` 的训练沿用第二问：附件二 train 共 3395 条，valid 共 728 条；训练时对原输入及连续局部缺失输入同时施加加权交叉熵与 Huber 回归损失，另以权重 0.1 加入可观测单模态辅助头损失。实际训练目标为

$$
L=L_{\mathrm{CE}}^{\mathrm{clean}}+L_{\mathrm{Huber}}^{\mathrm{clean}}
 +L_{\mathrm{CE}}^{\mathrm{mask}}+L_{\mathrm{Huber}}^{\mathrm{mask}}
 +0.1\,\frac{L_{\mathrm{aux}}^{\mathrm{clean}}+L_{\mathrm{aux}}^{\mathrm{mask}}}{2}.
$$

三路均可被随机选作缺失增强对象；文本被遮蔽时先替换相应 token 再经固定 BERT 重编码。采用 AdamW、学习率 $3\times10^{-4}$，三个随机种子均训练 30 轮，分别保留验证准则最优的第 5、7、4 轮权重。最终对三个模型的类别概率和强度分别取算术平均。第三问不声称新训练相对第二问获得了性能提升。

### 2. 模态作用量化

设 $M=\{T,A,V\}$ 为三种模态的集合，对每条样本固定完整输入所预测的类别 $c=\operatorname*{argmax}_k p_k(X)$。对任意模态子集 $S\subseteq M$，以参考值替换不在 $S$ 中的模态特征，定义分类评价函数 $v_c(S)=p_c(X_S)$；强度评价函数为 $v_r(S)=\hat y(X_S)$。三模态仅有八种子集，可以完整枚举。模态 $m$ 的组级 Shapley 贡献定义为：

$$
\phi_m^{(q)}=\sum_{S\subseteq M\setminus\{m\}}
\frac{|S|!(2-|S|)!}{3!}\left[v_q(S\cup\{m\})-v_q(S)\right],
\qquad q\in\{c,r\}.
$$

其中 $\phi_m^{(c)}$ 度量该模态对**当前预测类别概率**的带符号作用，$\phi_m^{(r)}$ 度量它对强度数值的带符号作用；二者不能混用。主分析以附件二训练集有效文本向量均值、音视频经训练集标准化后的零向量作参考，保持原始有效位置标记。再以“将整模态标为不可用”为另一参考进行敏感性检查。程序验证 $\sum_m\phi_m=v(M)-v(\varnothing)$；若最大正向 $\phi_m^{(c)}$ 对应的模态存在有效输入，就将其记为该样本的主要参考模态。这里的贡献是模型对参考输入的响应，不能解释为人物真实情绪的因果比例。Shapley 思想参考文献[2]；本文实施的是三个**模态整体**的精确枚举，而非调用通用 SHAP 软件给 877 个特征维度逐个赋值。

### 3. 关键证据定位与核验

先用附件四提供的 `raw_text` 和 `text_bert` 核对原文与 BERT 子词位置；20 条样本的 token ID 均完全吻合。按连续三个原词构造候选窗口，分别对文本、音频和视觉窗口用训练参考值替换，定义局部支持度 $d_{m,I}=p_c(X)-p_c(X_{m,I\leftarrow\mathrm{ref}})$。只保留 $d_{m,I}>0$ 的窗口，各模态取前两段互不重叠的候选。文本还进行第二层检查：将对应原词的全部子词替换为 `[MASK]`，通过同一冻结 BERT 重新编码后再预测，记录原类别概率变化。这样可以区分“缓存特征被改动后敏感”与“原词被遮蔽并重新编码后仍敏感”。两者不一致时不把该词写成可靠语义证据。

原文字符范围由分词器直接提供；语音秒数通过原文与附件四原视频音轨的 CTC 强制对齐重建。视觉候选在对应的估计时段从原视频按真实帧 PTS 抽取关键帧，并保存回看入口。附件四没有官方音视频特征行时间戳，因此音视频行与原视频秒数的关系属于**基于文本对齐接口及音轨重建的估计**，必须通过原视频人工核对；置信度不足的窗口不填秒数。这样的扰动检查借鉴了解释忠实性评价的思路[3]，但本文未使用 ERASER 的人工证据标签，也不报告其官方证据定位分数。

### 4. 验证结果

在附件二完整验证集 728 条样本上，固定模型的三分类 Accuracy 为 **0.6374**，Macro-F1 为 **0.6173**；负向、中性、正向 F1 分别为 **0.6651、0.4783、0.7084**。情感强度的 MAE 为 **0.5984**，Pearson 相关系数为 **0.6469**。这组指标衡量复用预测器在有标签数据上的表现，不属于附件四准确率，也不能宣称解释方法提高了分类性能。

为检查文本证据的模型敏感性，预先按验证集真实类别分层抽取 128 条，其中 5 条文本过短、缺少两个可比较窗口，实际分析 123 条。对每条先按特征窗口扰动选第一名，再分别对第一名与另一处随机三词窗口进行原词遮蔽和 BERT 重编码。第一名使原预测类别概率平均下降 **0.1432**，随机窗口平均下降 **0.0518**；在 **73.17%** 的样本中，第一名的下降幅度大于随机窗口。该检验采用不同于排序阶段的文本重编码扰动，但仍基于同一预测器选窗口，并非人与模型共同标注的证据准确率，也不能外推为音频、视觉定位质量。

### 5. 附件四推理与示例

对附件四全部 20 条样本完成最终推理，模型输出负向 9 条、中性 4 条、正向 7 条。依据主参考下的最大正向类别贡献，18 条的主要参考模态为文本，2 条为视觉，0 条为音频。此结果表明该固定模型在附件四上明显依赖文本，不能写成“三模态贡献均衡”。另一参考规则使 3 条样本的主模态排序变化，应在个例解释中保留参考方式敏感性。

以样本 **10** 为例，模型预测负向概率 `0.890`、强度 `−1.595`，文本对负向类别的组级贡献为 `+0.861`。候选文本“**was pretty terrible**”对应重建时段约 `7.51–9.21 s`；遮蔽原词并重新编码后，负向概率降低 `0.274`。该案例展示了模型给出的可复核文本依据，词语在原视频中出现的具体时刻仍由人工回看确认。样本 **05** 预测正向概率 `0.618`、强度 `+0.415`，视觉对正向类别贡献 `+0.056`，可回看约 `1.26–2.25 s` 的视觉候选与真实视频帧。同一样本的视觉对强度数值贡献为 `−0.021`，说明**极性贡献与强度贡献不能混为一谈**。这些结果只能说明整路视觉特征影响了预测，不能由此推断模型具体识别了“微笑”或某一种表情。

共输出 75 条正向支持的局部候选：文本 39、音频 24、视觉 12；69 条具备可回看的重建时间，6 条因对齐置信度不足而不填写秒数。附件四 **13** 号视觉有效行数为零，故不把视觉作为该样本的证据。样本 **19** 的类别判为负向 `p_-=0.458`，但强度为 `+0.133`，且正向类别概率 `p_+=0.431` 与负向接近；这是极性与强度不一致、需要人工审查的案例，不能在无标签条件下直接判定模型正确或错误。附件四没有任何情感真值或人工证据标签，因此这里只报告预测分布和可复核案例，不报告附件四 Accuracy、F1 或证据定位 F1。

### 6. 方法局限

第一，模态贡献依赖参考输入，本文保留均值参考和显式缺失参考的两套结果，避免把某一次计算写成绝对作用比例。第二，三词窗口遮蔽会构造非自然输入，文本重编码也只是模型行为检验；挑出窗口与检验窗口仍存在选择偏差。第三，音视频 aligned 特征不附原始秒数，CTC 词时刻只构成估计映射；需逐条回看原视频。第四，视觉 35 维未附逐维表情语义字典，不能将整路视觉贡献解释成“表情贡献”；样本 13 还存在视觉全零情况。第五，附件四无标签，全部 20 条结论需要明确区分“模型预测”和“事实情绪”。

## 二、论文排版建议与数据来源

推荐放 1 张技术路线图、2 张图、2 张表即可：

1. **技术路线图**：三模态有效位置编码 → 双任务输出 → 八子集 Shapley → 三词窗口扰动 → CTC/视频回溯。
2. **表 1：验证集预测性能**：使用上文 Accuracy、Macro-F1、三个类别 F1、MAE、Pearson；注明 `n=728`、`C_uni` 三种子集成。
3. **图 1：案例 10 的三模态有符号贡献**：从 `modality_contributions.csv` 的 `reference=mean`、`sample_id=10` 取 `class_text/audio/vision`；纵轴为原预测类别概率贡献，允许负值。
4. **图 2：案例 10 的证据时间轴**：从 `key_evidence.csv` 取原文、重建秒数和局部变化，配合 `人工审查.html` 截图。若图中展示案例 05 的视觉帧，图注写“原视频抽帧、时间为重建估计”。
5. **表 2：附件四示例与风险案例**：建议列 10、05、13、19 号，区分预测、主要模态、局部证据、审查备注。附件四全量 20 行预测另作附件，不在正文用其类别比例当成绩。

论文可将上文压缩为“模型构建—解释计算—验证与实例—局限”四段。表格和图必须直接从下面的结果文件读取，人工核对后再写正式图注：

- `results/q3_c_uni_final_20260925/predictions.csv`
- `results/q3_c_uni_final_20260925/modality_contributions.csv`
- `results/q3_c_uni_final_20260925/key_evidence.csv`
- `results/q3_c_uni_validation_full2_20260925/summary.json`

**交稿前核对：**此文中的视频时间和关键帧仍标为估计。`results/q3_c_uni_final_20260925/人工审查记录.csv` 尚待填写；在完成回看前，不要把“模型认为关键”写成“人工确认关键”。

## 参考文献建议

[1] Bagher Zadeh A, Liang P P, Poria S, et al. Multimodal Language Analysis in the Wild: CMU-MOSEI Dataset and Interpretable Dynamic Fusion Graph[C]//Proceedings of ACL. 2018. [原论文](https://aclanthology.org/P18-1208/).

[2] Lundberg S M, Lee S-I. A Unified Approach to Interpreting Model Predictions[C]//Advances in Neural Information Processing Systems. 2017. [原论文](https://proceedings.neurips.cc/paper/2017/hash/8a20a8621978632d76c43dfd28b67767-Abstract.html).

[3] DeYoung J, Jain S, Rajani N F, et al. ERASER: A Benchmark to Evaluate Rationalized NLP Models[C]//Proceedings of ACL. 2020: 4443–4458. [原论文](https://aclanthology.org/2020.acl-main.408/).
