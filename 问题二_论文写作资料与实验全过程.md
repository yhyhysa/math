# E 题问题二：论文写作资料与实验全过程

> **给撰写论文的队友。**本文件整理已实现的方法、真实实验结果、证据位置和表述边界，可用于撰写“问题分析—模型建立—求解与实验—敏感性分析—模型评价”。它不是本机操作手册；运行命令另见 [运行使用说明.md](运行使用说明.md)。文中“最终模型”指满足三模态任务要求后采用的工程方案，不表示在所有候选或外部数据上最优。统计截至 2026-09-24。**最终方案现为 C_uni：三模态分支 Transformer＋直接拼接＋单模态辅助监督，辅助损失权重 0.1，不含可靠性门控。**最终版本以 [配置记录](configs/q2_final.json) 为准。

## 1. 题目契约与研究目标

原题《复杂场景下多模态情感识别的数学建模与算法设计》要求在**文本、语音、视觉某一个或多个模态的局部连续时段缺失**时，同时预测情感极性和情感强度，并分析缺失模态、位置和长度造成的影响。附件二有 4850 条特征样本；本工程选用其 `aligned_50.pkl`，保留官方划分：train 3395、valid 728、test 727。附件三是 30 条无情感标签的对齐版专项样本，只用于最终预测。[题面逐条核对记录](results/q2_题面与隐藏文本复核_20260924.md)列出对应条款。

| 研究对象 | 本工程中的具体定义 | 论文中的注意点 |
|---|---|---|
| 三模态输入 | 每条样本至多 50 个对齐序列位置；文本 `50×768`、音频 `50×74`、视觉 `50×35` | 这里的 50 是词或子词序列槽位，含特殊符号与填充，**不是视频时长的 50 等分** |
| 极性输出 | 三分类：0 负面、1 中性、2 正面 | 不能把强度回归值的正负直接当成三分类预测；0 强度附近也不等于全部中性 |
| 强度输出 | 连续情感强度，合法区间 `[-3,3]` | 与极性共用表征、分别设置输出头 |
| 缺失 | 一个或多个模态的部分连续**有效序列位置**不可观测 | 不能把 padding、特殊 token、整模态永久删除和局部缺失混为一谈 |
| 数据用途 | train 拟合权重及音视频标准化；valid 选轮、比较结构和参数；test 仅作描述性检查；附件三最终推理 | 附件三无标签，不报告其 Accuracy、F1、MAE 或 Pearson；不引入其他情感数据训练 |

原数据中音视频某些有效位置本来就是全零。我们将其标记为**疑似不可观测**，而不宣称已查明官方制造缺失的原因。附件二 valid 中，85/728 条至少有一个音频或视觉有效位置的全零行；视觉涉及 85 条、音频涉及 1 条。[有效位置复核](results/q2_valid_missing_audit_20260924_v2/summary.json)。附件三 30 条里各有 27 条出现音频、视觉全零行；24 条各自出现两个及以上不相连零段。[附件三形态复核](results/q2_A3缺失形态复核_20260924.md)。这些是**观察结果**，附件三没有参与训练增强概率的选择。

## 2. 符号、假设与预处理

令 $i$ 表示样本，$t\in\{1,\ldots,50\}$ 表示对齐序列槽位，$m\in\{T,A,V\}$ 分别表示文本、音频、视觉。$x_{it}^{m}\in\mathbb R^{d_m}$，$d_T=768,d_A=74,d_V=35$。$y_i^{c}\in\{0,1,2\}$ 是极性标签，$y_i^{r}\in[-3,3]$ 是强度标签。

1. **内容位置。**读取 `text_bert` 的 token ID、attention mask、type ID。仅 attention=1 且位于 `[CLS]` 与 `[SEP]` 之间的位置记 $c_{it}=1$；其余位置不参与缺失生成和时序汇聚。单条样本实际有效内容长度可小于 50。
2. **可观测掩码。**文本的原始可观测位置为 $o_{it}^{T}=c_{it}$；音视频按 $o_{it}^{m}=c_{it}\,\mathbf1(\max_j|x_{itj}^{m}|>10^{-6})$ 判断。全零只是可复核的操作性判据，不代表必然是人为遮挡。[实现](src/q2_train_baseline.py)。
3. **标准化。**音频和视觉仅用 train 中可观测行拟合各维均值、标准差，标准差下限 $10^{-6}$；同一组参数用于 valid、test 和附件三。不可观测行标准化后仍乘掩码置零，避免把原来的零行变成非零值。文本沿用题给的 768 维特征，缺失视图需另行 BERT 重编码。
4. **局部缺失假设。**在内容位置上截取连续区间，遮蔽一个或两个模态；标签沿用原样本。仅对 train 生成随机训练视图，valid 使用预先固定的测试场景。该做法是本工程为了模拟题目现象作出的建模选择；题面没有逐字规定具体的增强分布。
5. **版本一致。**训练、验证、test 描述性检查和附件三推理均使用附件的对齐版接口。第一问从原视频自制的特征完全不进入本问。原附件、标签和划分没有被改写。

文字缺失时，不能仅把预计算的 768 维向量置零：BERT 上下文中未遮蔽位置也可能含有被遮词的信息。本工程将相应 token ID 替换为 `[MASK]`，由固定版本的 `bert-base-uncased` 重算该视图文本向量，再按可观测位汇聚；BERT 权重不在本问中更新。附件三只提供 `text_bert(3×50)` 而没有 `text(50×768)`，因此最终预测也由同一冻结 BERT 重建文本特征。数据文件和 BERT 权重的 SHA-256 分别固定为 `66e867aa…d97ff2cb8fcd` 与 `68d45e23…cc53d20e772753c6bb9169d3`，完整值见[历史验证对照记录](results/q2_tonight_20260924/selection.json)。

## 3. 模型构建

### 3.1 可解释的轻量基线 A

先对每个模态的有效序列位置作掩码均值池化，并附加有效覆盖率、是否仍有可用行的标志，再送入共享 MLP。输出层包括三类 logits 与一个回归值 $\hat y_i^r=3\tanh(z_i)$。这个基线用于确认数据、损失和缺失模拟能否跑通，也用于文本 T、文本＋音频 TA、文本＋视觉 TV、完整三模态 TAV 消融。[代码](src/q2_train_baseline.py)。TV 在部分验证指标上更高，但**不读音频**，因此只能是消融对照，不能作为回答“音频缺失会怎样”的完整三模态最终模型。

### 3.2 最终三模态时序模型 C_uni

最终采用三条独立的浅层时序编码分支：[实现](src/q2_train_transformer.py)。每个模态先线性投影至 96 维并作 LayerNorm，加 50 位位置嵌入，再经过 **1 层 Transformer Encoder**（4 个注意力头、前馈层 192 维、dropout 0.1）。注意力屏蔽该模态不可观测位置；若整个模态无可观测内容，编码器临时保留一个安全位置以避免数值异常，但该模态最终池化表示仍为零。设编码输出为 $z_{it}^{m}$，则

$$h_i^m=\frac{\sum_t o_{it}^{m} z_{it}^{m}}{\max(1,\sum_t o_{it}^{m})},\qquad
\rho_i^m=\frac{\sum_t o_{it}^{m}}{\max(1,\sum_t c_{it})}.$$

最终模型直接拼接 $[h_i^T;h_i^A;h_i^V;\rho_i^T;\rho_i^A;\rho_i^V]$，进入 128 维共享隐藏层（ReLU、dropout 0.1），再分别接三分类头与 $3\tanh$ 回归头。**融合采用直接拼接，不使用可靠性门控。**

此外，在三个 96 维单模态表示 $h_i^m$ 上分别接一个独立的线性辅助头，输出 3 个分类 logits 和 1 个经 $3\tanh$ 限制范围的强度值。每个辅助头均用真实标签监督，促使单个模态保留可用于情感预测的信息。它属于单模态辅助监督，不包含教师模型或蒸馏损失。该模型共 **353264 个可训练参数**，比无辅助头版本的 352100 个增加 1164 个。

三个种子各自使用 valid 上最佳轮权重。推理时仅取各模型的融合输出，对三份分类概率和三份强度分别等权平均。**辅助头的预测不参与最终投票；训练时通过辅助损失更新三条编码分支。**

### 3.3 训练缺失视图与损失

最终模型每轮对每条 train 样本额外生成一个随机的**局部连续缺失视图**。原方案以 0.75 概率抽单模态，以 0.25 概率抽双模态，双模态为 TA、TV、AV；在有效内容长度的 10%、30%、50% 中等概率抽长度，再随机抽起点。若拟遮区间原本没有可观测特征，或会使三模态全部不可用，则重新抽样；最终三种子训练的记录每轮 3395 条均发生实际有效遮蔽、回退数为零。音视频内部可用性掩码与输入特征对应行置零，经 16 条 valid 的 A/V/AV 试验验证预测一致。[等价性核验](results/q2_step7_input_equivalence_step8_20260924.json)。文字按上节冻结 BERT 的 `[MASK]` 路径重编码。

令 $L_{\mathrm{main}}(v)$ 为视图 $v$ 的融合输出损失，即加权交叉熵与 $\delta=1$ 的 Huber 强度回归损失之和。类别权重由 train 类别频数的平方根倒数计算，并归一化至均值为 1；分类损失按批内真实类别权重之和归一化，回归损失按样本数取均值。

对一个训练批次，令 $B_m(v)$ 为模态 $m$ 仍有可观测内容的样本集合，$\mathcal M(v)$ 为 $B_m(v)$ 非空的模态集合。辅助损失为

$$L_{\mathrm{aux}}(v)=\frac{1}{|\mathcal M(v)|}
 \sum_{m\in\mathcal M(v)}\frac{1}{|B_m(v)|}
 \sum_{i\in B_m(v)}
 \left[-w_{y_i^c}\log p^{m}_{i,y_i^c}(v)
 +L_{\mathrm H}\!\left(\hat y_i^{r,m}(v),y_i^r\right)\right].$$

若某个模态在该样本中完全不可观测，跳过对应样本的该模态辅助损失；如果没有可监督模态，则辅助损失取零。实现中的辅助分类项按有效样本数平均加权逐样本损失，与主分类项的归一化方式有所区别。完整视图与连续缺失视图共用网络参数，最终目标为

$$L=L_{\mathrm{main}}(x)+L_{\mathrm{main}}(\tilde x)
 +0.1\,\frac{L_{\mathrm{aux}}(x)+L_{\mathrm{aux}}(\tilde x)}{2}.$$

优化器 AdamW，学习率 $3\times10^{-4}$、weight decay 0.01、batch size 64。三个种子为 20260923/24/25，均实际训练 30 轮，分别使用第 **5、7、4 轮**保存的最佳权重。最终包含权重为 **0.1** 的单模态辅助监督，不包含课程式缺失率、预测一致性、可靠性门控或门控正则。[最终模型训练摘要](results/q2_c_aug_1layer_concat_eff_tonight_uni_20260924_seed_20260923/summary.json)记录了参数、命令、输入哈希和最佳轮次。

## 4. 评价方案与实验可比性

原样 valid 和人工模拟缺失 valid 均报告 Accuracy、三分类宏平均 F1、情感强度 MAE 与 Pearson。固定缺失面板为 **6 类模态组合 T/A/V/TA/TV/AV × 3 个内容序列位置（前/中/后）× 3 档内容长度比例（10%/30%/50%）=54 场景**。前/中/后按**内容 token 顺序**定义，不能写成物理视频的前/中/后秒数；单个样本的比例因向上取整、原有全零行而与名义值不完全相同。

用于选轮和同训练预算对照的本地综合分为

$$J=0.25\left(\frac{\mathrm{MAE}_{c}}{3}+1-\mathrm{F1}_{c}\right)
  +0.75\left(\frac{\overline{\mathrm{MAE}}_{54}}{3}+1-\overline{\mathrm{F1}}_{54}\right),$$

其中下标 $c$ 为原样 valid，上横线表示 54 场景等权平均，**越小越好**。这只是训练前设定的本地多目标选型规则，**不是主办方官方指标或最终评分公式**。每种方案优先作同种子配对，再看三种子均值、标准差和各单项指标；54 场景复用相同的 728 条 valid，不能当作 54 个独立样本集作显著性推断。本文报告的候选比较以 valid 为准。最终方案是在比较实验后确认的 C_uni，不应描述成预设综合分规则自动选出的胜者；本文件不提供它的独立盲测成绩。附件三完全没有可评价标签。

## 5. 完整实验过程：做了什么、为什么、结果如何

下面按实际推进顺序列出**已执行**的训练和 valid 对照。各阶段的详细表、协议与训练记录链接放在末尾证据索引。所有 J 数字均为附件二 valid 三种子均值；不同阶段的 J 由当时实际模型配置产生，不能把历史小差异写成独立总体结论。

| 阶段 | 问题与单一主要改动 | 主要结果或发现 | 建模决策 |
|---|---|---|---|
| 数据核验与首个 MLP | 统一读取附件二，先用三模态池化 MLP 打通三分类＋回归 | train/valid/test 为 3395/728/727；首次单次 8 轮早停仅作通路检查，后续公平对照改为三种子各 30 轮 | 不把速度 demo 或 8 轮早停当最终成绩 |
| 连续缺失增强 | 同一 MLP，比较完整视图训练与完整＋随机连续缺失训练 | TAV 的 J `0.6226 → 0.6200`，三种子方向不全一致；缺失 F1 略升 | 保留增强作为待检验机制，不宣称已稳定提升 |
| 时序模型 B | 早融合 token 级门控＋1/2 层 Transformer，与 MLP 同预算比较 | 最佳 2 层增强 B 的 J `0.6269`，逊于当时 MLP `0.6200`；训练损失下降而 valid 后期回落 | 检查掩码与架构设置，不认为“模型更复杂必更好” |
| 模态消融与掩码修复 | 比 T/TA/TV/TAV；修复 Transformer 中全缺失位置的注意力参与 | 增强 TV 的 J `0.6097`，但旧增强有对 TV 无效的“只遮音频”；B 修复前后 `0.6269/0.6263`，改善很小 | TV 仅作两模态参照，不能回答音频缺失；修复保留 |
| 不加增强的模态价值复核 | 移除扰动，公平比较 T/TA/TV/TAV | TV 对 T 的 J 在三种子均改善；均值 `0.6136` 对 `0.6235`；TA 的均值 `0.6319` | 画面在当前 MLP 下提供补充；不能推断音频普遍无效 |
| 有效遮蔽抽样 | 修正 TV 训练，使每个遮蔽确实作用于 T/V | 新 TV J `0.6150`，旧 TV `0.6097`；新规则更有效但成绩未提高 | 过程正确与分数提高分开报告 |
| 三模态分支时序模型 C | T/A/V 独立时序编码；直接拼接和覆盖率门控 | 学习率 `1e-3` 时直接拼接 J `0.6286`、门控 `0.6334` | 对直接拼接单独测试较低学习率 |
| 学习率控制 | 只将 C 直接拼接的 `1e-3` 改为 `3e-4` | J `0.6286 → 0.6141`；三个种子方向一致；缺失 F1 `0.5793 → 0.5889` | 固定三模态 C 的 `3e-4` 作为后续对照基点 |
| 预测一致性 | 在 C 的完整／遮蔽输出间加固定系数 0.5 的一致性项 | J `0.61408 → 0.61389`，平均仅 `0.00020`，种子方向不一致，缺失 MAE 略差 | 不纳入最终模型，也不写成稳健提升 |
| EMOE 启发的集中对照 | 在相同 C 基点上分别试 `1e-4`、同学习率门控、单模态辅助监督、二者叠加、一个或两个分离连续段 | 见下表；五种改动均未通过事前设定的稳定提升门槛 | 原预设规则保留无辅助头版本；按最终确认采用 C_uni，不宣称其验证性能稳定提升 |
| 最终方案确认与附件三推理 | 采用 C_uni 的三份现有最佳权重：直接拼接、单模态辅助损失权重 0.1 | valid 成绩与无辅助头版本接近；重新生成附件三 30 条预测，负面 6、中性 9、正面 15 | 最终模型和输出以 configs/q2_final.json 为准，保留历史实验记录 |

最后五项对照的程序与[训练前协议](results/q2_tonight_20260924/实验协议.md)已保存。每项三个种子、各 30 轮、同一 54 场景，新增 15 次正式训练全部退出码 0。这里借鉴 [EMOE 作者仓库](https://github.com/fuyyyyy/EMOE)的“样本级动态模态权重与单模态预测能力”动机；实际只实现了**覆盖率门控和单模态辅助头的受控变体**，没有复现论文完整的专家混合与单模态蒸馏机制，论文不能声称“完整复现 EMOE”。

| 三模态方案 | J ↓ | 原样宏 F1 ↑ | 54 场景宏 F1 ↑ | 54 场景 MAE ↓ | 同种子 J 改善数 |
|---|---:|---:|---:|---:|---:|
| C_base：无辅助头直接拼接，`3e-4` | **0.61408** | 0.61639 | **0.58887** | 0.63503 | 基准 |
| 同学习率可靠性门控 | 0.61561 | 0.60954 | 0.58247 | 0.62067 | 1/3 |
| 直接拼接，降至 `1e-4` | 0.61959 | 0.61185 | 0.58471 | 0.63925 | 1/3 |
| **C_uni：直接拼接＋单模态辅助头（最终）** | 0.61432 | **0.61705** | 0.58837 | 0.63519 | 1/3 |
| 门控＋单模态辅助头 | 0.61775 | 0.60736 | 0.57985 | **0.61961** | 1/3 |
| 直接拼接＋一或两个分离缺失段 | 0.61819 | 0.61190 | 0.58672 | 0.63892 | 1/3 |

**门控存在真实取舍。**门控加辅助头使缺失强度 MAE 比无辅助头基准低约 `0.01542`，但缺失分类宏 F1 从 `0.58887` 降为 `0.57985`，J 反而上升。不能只挑 MAE 宣称“门控全面有效”，也不能说它在所有指标都无用。`1e-4` 的 J 比 `3e-4` 高 `0.00551`，只有 1/3 个种子改善。因此沿用学习率 `3e-4`。本轮预设替换条件为 J 均值至少降低 `0.005`、至少两个同种子改善、缺失 MAE 恶化不超过 `0.01`；五种改动都不满足。[全部种子及差值](results/q2_tonight_20260924/all_seed_metrics.csv)可逐项复核。

**最终选择与实验结论分开表述。**C_uni 的 valid J 为 0.61432，无辅助头基准为 0.61408，差 0.00024；该结果不支持辅助监督获得稳定性能提升。C_uni 是比较完成后确认的最终方案。历史 selection.json 中 C_current 是当时规则的真实输出，保留原记录；当前最终配置见 configs/q2_final.json。论文可以说明本模型采用辅助监督，并报告与无辅助头版本的差异，不能编写“通过预设门槛”或“显著改善”的结论。

## 6. 分场景敏感性与模型评价

最终 C_uni 在 valid 原样条件下，三个种子均值为 Accuracy **0.63690**、宏 F1 **0.61705**、强度 MAE **0.61271**；54 场景等权平均为 Accuracy **0.60929**、宏 F1 **0.58837**、MAE **0.63519**，综合 J **0.61432**。[候选均值表](results/q2_tonight_20260924/candidate_means.csv)中筛选 C_uni 行。这些数值是三个单模型指标的算术平均。

三种子概率等权集成重新在 728 条 valid 原样样本上评价后，Accuracy **0.63736**、宏 F1 **0.61726**、中性 F1 **0.47826**、MAE **0.59843**、Pearson **0.64691**。集成成绩与三个种子的指标均值不能混用。[集成诊断](results/q2_final_c_uni_20260924/neutral_summary.json)和[中性类混淆表](results/q2_final_c_uni_20260924/neutral_confusion.csv)对应当前 C_uni。184 条真实中性中，88 条判对、38 条误判负面、58 条误判正面，中性仍是弱项。

54 场景中，C_uni **文字未遮蔽**组的平均宏 F1/MAE 为 **0.61537/0.61407**，**文字参与遮蔽**组为 **0.56136/0.65631**，反映该模型在当前人为扰动下仍依赖文本。[按缺失组汇总](results/q2_tonight_20260924/missing_group_means.csv)中筛选 C_uni，再对照 text_available 与 text_missing。不能据此推论音频或视觉在其他任务上没有作用。

本节仅报告最终模型的 valid 指标和无标签附件三输出；旧版本的指标不得挪用为 C_uni 的成绩。当前没有在本文报告的独立最终盲测证据，不能把验证集上的小差异表述为稳定泛化收益。

## 7. 论文写作建议：图表、可写结论与不可写结论

建议正文按“输入与缺失判据 → 三分支编码和双任务损失 → 固定 54 场景 → 基线、消融、学习率与增强对照 → 附件三预测 → 局限性”的顺序组织。可画模型结构图：三路输入与可观测掩码分别进入时序编码器，掩码池化后直接拼接，输出三分类和强度；三个池化后的单模态表示还各接一个辅助分类／回归头，以虚线表示仅参与训练损失。最终结构图不含可靠性门控或 EMOE 蒸馏模块。

| 建议图表 | 使用的已上传证据 | 图注必须交代的口径 |
|---|---|---|
| 三模态方法流程图 | 第 3 节公式、`src/q2_train_transformer.py` | 50 个槽位是对齐序列位置，不是视频等秒切片；音视频全零是疑似不可观测 |
| 有无连续缺失增强对比表 | [第二步结论](results/q2_step2_20260924/人工审查结论.md)、[第二步配对表](results/q2_step2_20260924/paired_seed_metrics.csv) | 同种子配对；小平均改进不等于稳定提升 |
| 模态消融表 | [无增强模态对照](results/q2_step6_20260924/candidate_means.csv) | TV 只作两模态消融，不能代表最终任务模型 |
| 时序模型、学习率与一致性表 | [第八步](results/q2_step8_compare_all_20260924/candidate_means.csv)、[第九步](results/q2_step9_compare_lr_20260924/candidate_means.csv)、[第十步](results/q2_step10_compare_consistency_20260924/candidate_means.csv) | 同时列 F1 与 MAE；J 为本地选型分 |
| 最终候选与门控取舍表 | [今晚六方案均值](results/q2_tonight_20260924/candidate_means.csv)、[配对差值](results/q2_tonight_20260924/paired_deltas.csv) | 门控 MAE 下降但 F1 下降；不作全面优劣宣称 |
| 不同缺失类型、位置、长度图 | [最终模型三个种子的 valid 场景明细](results/q2_c_aug_1layer_concat_eff_tonight_uni_20260924_seed_20260923/scenario_metrics.csv) | 同一数据被重复遮蔽、横轴是 token 相对位置和比例 |
| 中性类混淆矩阵 | [valid 3×3 计数](results/q2_final_c_uni_20260924/neutral_confusion.csv) | 集成模型的 728 条 valid 原样结果，不等于三种子指标均值 |

**可以写入结果段的谨慎表述：**“在附件二固定划分与 54 种预定义连续缺失场景中，分模态时序编码模型在学习率从 $10^{-3}$ 调至 $3\times10^{-4}$ 后，三种子平均综合验证分由 0.6286 降至 0.6141。进一步的样本级门控、单模态辅助监督和分离多段遮蔽没有通过事先设定的稳定改进规则。最终方案采用直接拼接并增加单模态辅助监督，valid 综合分为 0.61432，与无辅助头版本接近。该设计未显示出稳定的验证性能提升；模型对文字缺失更敏感，中性类辨识仍弱。”

**必须保留的限制：**没有官方缺失来源标签，无法证明原有零行的成因；验证场景是人为模拟，不能等同真实视频遮挡；反复比较同一 valid 可能使所选分数乐观；只有三个种子，没有独立置信区间；附件三无标签，无法验证最终 30 条预测的正确率；当前不报告独立最终盲测结论；未证明音频普遍无用，也未完整复现 EMOE。论文不要把未实施的“课程式缺失率、门控稳定正则、表示一致性”写进最终方法。

## 8. 可追溯证据与仓库边界

当前主模型为 [configs/q2_final.json](configs/q2_final.json) 中的 C_uni。旧实验表中的 C_current 指无辅助头对照，名字保留以便追溯，不表示仍是当前交付方案。

- **题面与输入：**赛题原始 DOCX 在本机原题目录；[题面条款与隐藏文本复核](results/q2_题面与隐藏文本复核_20260924.md)、[数据读取器](src/data.py)、[配置](configs/base.json)。原始附件没有上传到 GitHub，不可声称克隆仓库即可从零重训。
- **方法源码：**[池化基线与缺失抽样](src/q2_train_baseline.py)、[时序模型与训练](src/q2_train_transformer.py)、[多段遮蔽](src/q2_missing_patterns.py)、[附件三推理](src/q2_predict_attachment3.py)。各专项对照及检查代码在 `src/q2_*.py`、`src/run_q2_*.ps1`。
- **逐次训练记录：**`results/q2_*_seed_*/` 中保存已执行正式实验的各次 `summary.json`、`history.csv`、`mask_history.csv`、`scenario_metrics.csv` 和退出码；历史 MLP、早融合 Transformer、三模态分支模型均可按实验阶段与种子追溯。临时终端日志不随仓库上传。
- **最终权重：**本机 `checkpoints/q2_c_aug_1layer_concat_eff_tonight_uni_20260924_seed_{20260923,20260924,20260925}/best.pt`。按工程规则，权重、原始数据、预训练 BERT 和环境目录**不进入 Git**；只上传方法源码、配置、指标、样本预测与可核验的小型记录。室友写论文可以直接使用已上传表格；重新推理需从项目成员处取得原附件、固定 BERT 与三份权重。
- **最终推理：**[附件三 30 行预测](results/q2_attachment3_candidate_q2_c_aug_1layer_concat_eff_tonight_uni_20260924/predictions.csv)、[预测数量与状态](results/q2_attachment3_candidate_q2_c_aug_1layer_concat_eff_tonight_uni_20260924/summary.json)、[输入哈希清单](results/q2_attachment3_candidate_q2_c_aug_1layer_concat_eff_tonight_uni_20260924/input_manifest.csv)。样本 ID 来源于本地附件三文件名；三类概率未经单独校准。

## 参考文献与归属

1. 竞赛原题：《复杂场景下多模态情感识别的数学建模与算法设计》，第二十三届中国研究生数学建模竞赛 E 题，2026。论文引用时以原题 DOCX 为准。
2. Bagher Zadeh A, Liang P P, Poria S, et al. [Multimodal Language Analysis in the Wild: CMU-MOSEI Dataset and Interpretable Dynamic Fusion Graph](https://aclanthology.org/P18-1208/). ACL, 2018: 2236–2246. 用于说明 CMU-MOSEI 来源，不代表本工程使用了论文里的 DFG 模型。
3. Devlin J, Chang M W, Lee K, et al. [BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding](https://arxiv.org/abs/1810.04805). 2018. 本工程仅使用冻结的 `bert-base-uncased` 重建或遮蔽文字特征。
4. Fang Y, Huang W, Wan G, et al. [EMOE: Modality-Specific Enhanced Dynamic Emotion Experts](https://github.com/fuyyyyy/EMOE). CVPR, 2025. 今晚实验只借鉴样本级模态权重、保留单模态预测能力的思路；最终采用单模态辅助监督作为本题适配，未使用 EMOE 专家混合或单模态蒸馏，不能将两者视为相同方法。
