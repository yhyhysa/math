# E 题工程：我自己怎样运行、看结果、人工审查

这份说明是给你在这台电脑上直接操作的。所有命令复制到 Windows PowerShell 中运行。第一问仍看 q1_v2；**第二问当前主模型是 C_uni：三模态 Transformer＋直接拼接＋辅助头，辅助损失权重 0.1，没有门控。**它使用现成的三个最佳权重，不需要为了更新论文重新训练。最终模型记录在 configs/q2_final.json。

## 第二问当前版本：先打开这些结果

下面四行直接复制即可。第一份是你室友写论文要看的文档；第二份是当前主模型对附件三的 30 条预测；第三份记录本次版本更新。

~~~powershell
Set-Location -LiteralPath 'D:\第二十三届中国研究生数学建模竞赛 - 中文题目\中文题目\E题\工程'
Invoke-Item '问题二_论文写作资料与实验全过程.md'
Invoke-Item 'results\q2_attachment3_candidate_q2_c_aug_1layer_concat_eff_tonight_uni_20260924\predictions.csv'
Invoke-Item 'results\q2_final_c_uni_20260924\最终方案说明.md'
~~~

预测表的 polarity 为 negative/neutral/positive，strength 是 [-3,3] 的情感强度，三个 p_ 列是三类概率。附件三没有真值，这些概率不代表已验证的正确率。要看 valid 上的中性类错误，打开 results\q2_final_c_uni_20260924\neutral_confusion.csv；集成 Accuracy 为 63.74%，与三次单模型平均 63.69% 的口径不同。

**重新计算的前提：**本机原始附件、固定 BERT 和三个 C_uni 的 best.pt 均完整。已有同名结果时，程序拒绝覆盖；本机只打开结果即可。以下是用于完整工程副本首次复现的命令，逐条执行且退出码应为 0。完整性核验应显示 PASS、245 项。

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_predict_attachment3 --model C_uni
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_neutral_diagnostic --candidate C_uni --output-name q2_final_c_uni_20260924
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

附件三推理入口现在默认也是 C_uni；显式的 --model C 和 --model TV 留作历史版本重现。--candidate-prefix 配合 --model C 可指定其他已完成训练的三模态权重前缀。中性诊断的 --candidate 指定候选，--output-name 指定新的结果目录名，只允许字母、数字、下划线和短横线，且不覆盖已有诊断结果。

如果确实需要重新训练同结构，用下面命令；它用新标签保存三次各 30 轮训练，并逐轮打印损失和 valid 指标。权重写入 checkpoints，新记录写入 results。已有同名目录时不要反复运行，换一个新的 Tag。训练需要原始附件和 BERT，不能仅靠克隆 GitHub 运行。

~~~powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode full -Architecture separate -Fusion concat -Layers 1 -Augment -EffectiveMask -LearningRate 0.0003 -AuxiliaryWeight 0.1 -Tag final_uni_reproduce_20260924
~~~

后面的分步说明保留实验过程；旧 selection.json、旧附件三预测及“今晚交付”ZIP 属于历史材料。当前模型和最终预测以本节及 configs/q2_final.json 为准，旧打包程序 q2_package_tonight.py 仍按历史选择打包，不要把旧包当作当前 C_uni 交付件。GitHub 提供当前源码、说明及小型结果，原始数据、BERT 和 best.pt 仍在本机。

## 第 0 步：打开工程

打开 PowerShell，先粘贴这一行。以后本文件的命令都从这个位置执行：

~~~powershell
Set-Location 'D:\第二十三届中国研究生数学建模竞赛 - 中文题目\中文题目\E题\工程'
~~~

检查本机 Python 是否还在：

~~~powershell
Test-Path '..\训练demo\.venv\Scripts\python.exe'
~~~

显示 True 就能继续。显示 False 时，下面的 Python 命令跑不了；但已经生成的图片和表格仍可直接打开。即使 PowerShell 提示符显示 (base)，那只是本机 E:\Miniconda3 的默认 conda 环境；本文件的训练命令明确指向 D 盘工程的 .venv，所以不会用错 Python。命令中的样本 ID 带有美元符号，**整段 ID 一律用单引号**，否则 PowerShell 会把它当变量。

## 最快上手：只看现成结果

下面几行只打开文件，不会重新提取或训练：

~~~powershell
Invoke-Item 'results\q1_v2\q1_quality_proxy_dashboard.png'
Invoke-Item 'results\q1_v2\q1_review_figures\-571d8cVauQ$_$0_review.png'
Invoke-Item 'results\q1_v2\人工审查指引.md'
Invoke-Item 'results\q2_first_baseline\history.csv'
Invoke-Item 'results\q2_step3_20260924\人工审查结论.md'
Invoke-Item 'results\q2_step5_20260924\人工审查结论.md'
Invoke-Item 'results\q2_step6_20260924\人工审查结论.md'
Invoke-Item 'results\q2_step8_20260924\人工审查结论.md'
Invoke-Item 'results\q2_step9_20260924\人工审查结论.md'
Invoke-Item 'results\q2_step10_20260924\人工审查结论.md'
Invoke-Item 'results\q2_最终三模态修正回执_20260924.md'
Invoke-Item 'results\q2_第二问完整回执_20260924.md'
Invoke-Item 'results\q2_locked_test_20260924\独立测试结果.md'
Invoke-Item 'results\q2_attachment3_locked_20260924\人工审查说明.md'
Invoke-Item 'results\q2_attachment3_locked_20260924\predictions.csv'
Invoke-Item 'results\q2_attachment3_three_modal_20260924\predictions.csv'
~~~

第一张图看 100 段的词覆盖率和人脸检出率；第二张图看一段视频中“词、声音、人脸、原画面”的时间关系；《人工审查指引》告诉你先查哪些视频；后面的文件让你看第二问从早期基线到独立测试、附件三预测的结果。若打开 CSV 时进入 Excel，先只看，不要覆盖原文件。

**最重要的区分：**图上的 PASS 只表示自动检查通过。词是否卡在真正的发音位置、绿色人脸框是否框住正在说话的人，仍要你播放原视频判断。

## 第一问：照着图人工审查

### 1. 先查有风险的 32 段

~~~powershell
Invoke-Item 'results\q1_v2\q1_face_review_priority.csv'
Invoke-Item 'results\q1_v2_face_contact_sheets\-571d8cVauQ$_$0_center_crop_0.7.png'
Invoke-Item 'results\q1_v2\q1_review_figures\-571d8cVauQ$_$0_review.png'
~~~

优先表中前 27 段用了画面裁剪，后 5 段仍没有有效人脸。上面是一个裁剪后找到人脸的例子。绿色框应落在**当前音频的说话人**脸上；电视里的人、听众、路人也可能被算法框到，所以要听原视频。如果想看一个风险更明显的例子：

~~~powershell
Invoke-Item 'results\q1_v2\q1_review_figures\-HwX2H8Z4hY$_$9_review.png'
~~~

四层图从上到下是：估计的词时间、原声音量变化、人脸检出点、四张原始画面。蓝色词块表示自动接受，橙色表示低可信。声音曲线只能提示发声位置，**不能代替听音**。绿色人脸点也不能证明它是目标说话人。

要打开示例对应的原视频，复制这两行：

~~~powershell
$video = (Get-Content -LiteralPath 'data\processed\q1_v2\-571d8cVauQ$_$0.json' -Raw | ConvertFrom-Json).video_path
Invoke-Item $video
~~~

若提示找不到视频，说明 JSON 中记录的路径在当前电脑上不可用；先到题目原始视频文件夹找同一段 MP4。截图和特征不能还原声音，必须打开原视频听。

### 2. 记录你自己的判断

~~~powershell
Invoke-Item 'results\q1_v2\q1_manual_review_sheet.csv'
~~~

表中每段有一行“整段”检查，异常词或人脸问题另有行。主要填写“人工判定”“修正起点秒”“修正终点秒”“审查者”“备注”。建议把填写后的表**另存为新文件**，保留原模板。重点看：词是否和发音同步；有脸时是不是说话人；没有脸时画面是否本就没有可见说话人。填完实际判断后，才能计算人工词边界误差和目标人脸正确率。

目前新版处理了 100 段视频，约按每秒 10 帧检查，共检查 7,813 帧，其中 7,110 帧得到有效面部特征，仍有 5 段零人脸。这里的“帧”是当时读过的原视频画面；工程保留了时间戳和特征，没有另存 7,813 张图片。100 段里 37 段自动 PASS、63 段 REVIEW；这些数字不等于人工正确率。第一问结果**只用于第一问**，不进入第二、三问训练。

## 第一问：自己生成一张随机热图

这一步只用已经算好的特征，不需要重新读取原视频。复制三行：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_timeline_demo --variant q1_v2 --seed 23
$pic = (Get-Content 'results\q1_v2\q1_demo\last_demo.json' -Raw | ConvertFrom-Json).png
Invoke-Item $pic
~~~

每次使用同一个 seed 会抽到同一段。想换一段，把 23 改成别的整数；想指定视频，可以运行：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_timeline_demo --variant q1_v2 '--sample-id=-571d8cVauQ$_$0'
Invoke-Item 'results\q1_v2\q1_demo\-571d8cVauQ$_$0_timeline.png'
~~~

默认随机只从自动 PASS 且有人脸的片段抽取；加上 --include-review 可以把待复核片段也放进随机池。输出在 results\q1_v2\q1_demo\，有 PNG、SVG 和 last_demo.json；重复运行会更新 last_demo.json，也可能覆盖同名图片。

读图时：三排分别是文字 768 维、声音 70 维、画面 61 维，横轴是视频秒数；灰色表示该模态没有可用的时间特征。颜色只展示**同一段视频内部的变化**，不能拿两段视频的颜色深浅直接比较。每个词片的特征按词归并，然后铺在估计的词时间窗内；不是每 0.05 秒都重新提取了一遍。

## 第一问：自己重画“四层审查图”

这一步要读取原始 MP4，运行后不会修改已有特征。示例：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_review_figure --variant q1_v2 '--sample-id=-571d8cVauQ$_$0'
Invoke-Item 'results\q1_v2\q1_review_figures\-571d8cVauQ$_$0_review.png'
~~~

程序默认在视频约 16%、38%、62%、84% 的位置截四张画面。想看指定秒数，恰好填四个、且都在视频时长内：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_review_figure --variant q1_v2 '--sample-id=-571d8cVauQ$_$0' '--times=0.8,2.0,3.2,4.3'
~~~

结果写到 results\q1_v2\q1_review_figures\，同一 ID 的 PNG/SVG 会被重画。程序打印解码帧数、抽样帧数、人脸有效帧数和图片路径。已有的 32 段重点视频图片可直接看，不必为了看图而重跑。

## 第一问：重新算自动指标

两条命令只读现有 q1_v2 特征，重写自动报告：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_validate --variant q1_v2
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_quality_dashboard --variant q1_v2
Invoke-Item 'results\q1_v2\q1_quality_proxy_dashboard.png'
~~~

检查结果在 results\q1_v2\q1_validation.json、q1_review_queue.csv、q1_quality_proxy_metrics.csv、q1_quality_proxy_report.json 和仪表图 PNG/SVG。结构核验通过，说明 100 个 JSON/NPZ 的形状、掩码、数值和记录的哈希符合程序规则；词覆盖率和人脸检出率也只是**自动覆盖率**。它们不能衡量词边界差了多少秒，不能确认人脸身份。需要对比旧版时，把 --variant q1_v2 改成 --variant q1；不写该参数也默认旧版。

## 第一问：试人脸阈值和裁剪

这一步读取原视频和 D 盘的 MediaPipe 人脸模型，只生成试验表与带框拼图，**不改第一问特征文件**。用一个样本比较原画面和两种裁剪：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_face_threshold_probe '--ids=-571d8cVauQ$_$0' '--settings=original_0.5,center_crop_0.7,center_crop_0.5' --fps 10 --output-dir=q1_face_probe_mine --output=mine.csv
Invoke-Item 'results\q1_face_probe_mine\mine.csv'
~~~

默认不写参数时，会跑预设的 6 条零人脸样本加 1 条正常对照，结果在 results\q1_face_threshold_probe\threshold_probe.csv。--ids 可用逗号接多个精确 ID；--fps 默认 10；--settings 可选 original_0.5、relaxed_0.3、relaxed_0.2、center_crop_0.7、center_crop_0.5；--no-contact-sheets 可不生成图片。带框 PNG 也会出现在所选的 results 子文件夹里。CSV 中 detected_frames/sampled_frames 是检出比例。挑检出率高的方案前，必须打开拼图看绿色框是否框住说话人。此前对 24 条旧版零人脸片段的试验表在 results\q1_face_threshold_probe\all_zero_crop.csv；降低阈值未解决已抽查的 6 段，裁剪效果更明显。

## 第二问：先看已经跑过的训练

~~~powershell
notepad 'src\q2_train_baseline.py'
Invoke-Item 'results\q2_first_baseline\history.csv'
Invoke-Item 'results\q2_first_baseline\summary.json'
Invoke-Item 'results\q2_first_baseline\scenario_metrics.csv'
~~~

这节的**小型基线**是把文字、声音、画面的有效位置分别求平均，再交给两层 MLP，预测三类情感和 -3 到 3 的强度。分类头的原始类别 **0=negative、1=neutral、2=positive**；回归头单独预测强度，不能把强度大于等于 0 就当作 positive。这里先讲无增强版；下一节有同模型的连续缺失增强版，再下一节是 Transformer。它只用附件二的 train 学习，用 valid 选轮和看效果；附件二 test、附件三和第一问特征都没参与训练。54 个“缺失场景”是在 valid 上人为遮掉不同模态、位置和长度后检验稳健性；不能把这些场景当作真实缺失标签。

看文件时按这个顺序：

| 文件 | 你主要看什么 |
|---|---|
| history.csv | 每轮 train_loss 是否下降；valid 的 clean 与 masked 指标何时最好；selection_J 是否随后变差 |
| summary.json | best_epoch、epochs_ran、best.clean、best.masked_mean、seconds、checkpoint |
| scenario_metrics.csv | 最优轮在 54 个缺失场景下分别掉了多少分，尤其缺失文字时 |

Accuracy 和 macro-F1 越高越好；MAE 越低越好；Pearson 越高越好。selection_J 是我们为验证集选轮设的综合分，**越低越好，不是赛题官方指标**。现有首次训练第 8 轮早停，选第 3 轮：完整 valid 的 Accuracy 0.6291、macro-F1 0.5934、MAE 0.6101、Pearson 0.6370。为核对早停，又用三个随机种子各跑满 30 轮，最佳轮分别是 3、4、5，后面各轮没有超过各自最佳验证分数。已有记录在 results\q2_baseline_30ep_seed_20260923\、20260924\、20260925\。这说明它能做基线对照；还不能说明第二问做完了。

训练之所以曾在约 26 秒结束，是因为模型先把长序列压成每段 883 个数字，只有约 13 万个可训练参数。数据文件约 994 MB，但进入这版基线训练的数组约 11.4 MiB；最费时的是预先构造 54 个验证场景。这不是视频模型训练或微调 BERT。具体计时可看 results\q2_timing_audit_seed_20260923\summary.json。图示“3.2 E题”那份意见逐条复核在 results\图示3_2风险复核.md；可用 Invoke-Item 打开。

## 第二问：我自己跑一次，并在终端看每轮

先开一个**看得见的 PowerShell 窗口**。训练需要附件二 aligned_50.pkl 和本机 D 盘的 BERT 权重；程序先核对这两个文件，缺失或哈希不符就停止，不会自动下载。以下是小样本通路测试，只跑 1 轮，不用它判断模型效果；--run-name 防止盖掉已有报告：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_train_baseline --smoke --run-name q2_my_smoke_01
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

小样本测试结果在 results\q2_my_smoke_01\，权重在 checkpoints\q2_my_smoke_01\best.pt；它只用前 128 条 train 和 64 条 valid。

正式复跑 30 轮，可逐行复制下面命令。想再跑一次，把 q2_my_30ep_01 改成 q2_my_30ep_02，避免覆盖自己的上次结果。

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
New-Item -ItemType Directory 'results\q2_my_30ep_01' -Force | Out-Null
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_train_baseline --seed 20260923 --epochs 30 --patience 30 --run-name q2_my_30ep_01 2>&1 | Tee-Object -FilePath 'results\q2_my_30ep_01\terminal.log'
$LASTEXITCODE
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

终端先显示读取数据，接着可能有约 20 多秒在准备 54 个验证场景，然后每轮打印一行。$LASTEXITCODE 显示 0 表示训练命令正常结束；非 0 时保存 terminal.log 和报错信息。--patience 30 表示本次会跑满设定的 30 轮，最终仍取**验证分数最好的那一轮**，并非强行用第 30 轮。原始附件完整性检查前后都应显示 PASS；如果检查失败，不要把这次运行当作有效复现。

运行完打开：

~~~powershell
Invoke-Item 'results\q2_my_30ep_01\history.csv'
Invoke-Item 'results\q2_my_30ep_01\summary.json'
Invoke-Item 'results\q2_my_30ep_01\scenario_metrics.csv'
Invoke-Item 'results\q2_my_30ep_01\terminal.log'
~~~

最佳权重在 checkpoints\q2_my_30ep_01\best.pt。**现在重新运行**该程序时，每轮终端会打印训练总损失及分类/回归分项、完整 valid 和 54 个缺失场景平均的 Accuracy、macro-F1、MAE、Pearson、选轮分数 J、目前最佳轮次和耗时。先前已经生成的 q2_first_baseline/history.csv 仍保留当时较少的列，不能把旧表当成新程序的逐轮日志。这里所谓“测试效果”指 valid 验证效果；封存的 test 不用于每轮挑模型，附件三也没有可计算 Accuracy/F1 的真标签。

## 第二问第二步：连续缺失增强

这一步**沿用上面的同一个 MLP 模型和同一组 54 个 valid 验证场景**，只改训练数据：每条 train 样本保留原样，再随机选文字、声音、画面中的一种或两种，把一个连续的内容片段遮住，两个视图都用原标签训练。文字会先把对应 token ID 换成 [MASK]，再用本机冻结 BERT 重算；声音和画面则在对应位置视作缺失。原始附件、valid、test 和第一问特征都不会改。

先运行一个不读原始附件的快速自检，检查遮蔽是否可复现、是否碰到填充位、是否真的替换了文字 ID：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_aug_selfcheck
~~~

然后在**你能看到的 PowerShell 窗口**粘贴这行，跑 128 条 train、64 条 valid 的一轮通路测试。窗口会实时显示运行记录：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode smoke
~~~

小样本通路测试通过后，用同一个窗口粘贴下面这行，依次跑三个随机种子，每个最多 30 轮，并且不提前早停；每轮都会显示完整损失和 clean/缺失验证指标：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full
~~~

这两条命令由运行脚本自动在训练前后核对原始附件，逐轮打印到终端，并保存 terminal.log、history.csv、scenario_metrics.csv、summary.json、mask_history.csv 和最佳权重。小样本结果在 results\q2_aug_smoke_20260924\；全量结果分别在 results\q2_aug_20260924_seed_20260923\、q2_aug_20260924_seed_20260924\、q2_aug_20260924_seed_20260925\；最佳权重在相应 checkpoints 文件夹。每个结果目录的 exit_code.txt 显示 0 且前后原始核验 PASS，才算该次正常跑完。若目录已存在，脚本会停止防止覆盖；重新运行时给命令加上 -Tag 20260924_b 之类的新标记。

先看新旧同种子的 summary.json：best.clean 是完整 valid，best.masked_mean 是 54 个缺失场景的平均；macro-F1、Accuracy、Pearson 越高越好，MAE 和 J 越低越好。再打开 mask_history.csv，确认每轮都有单/双模态缺失、文字/声音/画面的新增丢失比例；最后看 scenario_metrics.csv 中缺哪种模态最伤结果。要和第一步公平比较，请拿 results\q2_baseline_30ep_seed_20260923\ 等三个同种子、同样跑满 30 轮的目录作对照。一次分数提高不等于已经证明稳健；三种种子和各缺失场景都要看。这一步仍是模型 A 的增强对照，Transformer 主模型属于后续步骤。

三次全量训练都显示 exit_code.txt 为 0 后，再复制这两行生成**新旧配对比较表**：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step2 --tag 20260924
Invoke-Item 'results\q2_step2_20260924\paired_seed_metrics.csv'
~~~

同一目录的 scenario_delta.csv 是 54 个场景跨三个种子的平均差，summary.json 是平均差及种子间波动。表中 delta = 增强版减原基线版：F1/Accuracy/Pearson 的正数、MAE/J 的负数才表示改善。脚本会先检查数据哈希、模型结构、样本数、30 轮、种子和 54 个场景一致；任一条件不一致会停止，不生成看似可比的结论。如果训练时用了新的 -Tag，这里的 --tag 和结果目录名也改成那个标记。

本轮三种子的简明判读在 results\q2_step2_20260924\人工审查结论.md；当时的输入文件哈希、Python/依赖版本、种子、参数和重跑命令保存在同目录的 复现清单.json。可以直接打开：

~~~powershell
Invoke-Item 'results\q2_step2_20260924\人工审查结论.md'
Invoke-Item 'results\q2_step2_20260924\复现清单.json'
~~~

## 第二问第三步：按时间顺序读特征的模型 B

这一步把每个有效内容 token 的文字、声音、画面先合成一个向量，再让小型 Transformer 按 token 顺序读取整段，输出仍是**负向/中性/正向三分类**和单独的强度。它会记录哪一种模态在当前位置还可用；三种都不可用时使用一个明确的空位表示。这里的“时间顺序”是附件二对齐后的 token 顺序，**不是视频等分成 50 段秒数**。模型 A 与 B 是整体方案对照，不能把差别全说成 Transformer 或门控单独带来的。

先做不读附件的快速自检，检查三类输出、空位、填充位隔离、梯度和强度范围：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_train_transformer --selfcheck
~~~

下面这行会**打开可见 PowerShell 窗口**，用 128 条训练、64 条验证做一轮最小试跑；每轮损失和验证效果在窗口里显示。默认先试 1 层、无增强：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode smoke -Layers 1 -Tag my_smoke_01
~~~

试跑通过后，下面这行依次跑 1/2 层、无增强/有增强共四种配置，每种三个种子，每次跑满 30 轮；这是完整对照，耗时明显长于模型 A。它在最后还会读取已经完成的六份模型 A 三种子结果，生成 A/B 比较表，所以这些旧结果和权重必须仍在本机。请保持可见窗口打开，直到比较结果也打印出来：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode grid
~~~

如果只想重跑其中一种配置，下面是**1 层有增强**的例子；改 `-Layers 2` 就是 2 层，删去 `-Augment` 就是无增强。每次重跑给 `-Tag` 换一个新名字，以免覆盖结果：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode full -Layers 1 -Augment -Tag my_try_01
~~~

全量结果在 `results\q2_b_clean_1layer_20260924_seed_20260923\` 等同样格式的文件夹，最佳权重在 `checkpoints\` 下的同名文件夹。每个文件夹的 `exit_code.txt` 必须是 0。`history.csv` 看逐轮损失与验证成绩；`summary.json` 看最佳轮、模型层数和完整/缺失指标；`scenario_metrics.csv` 看 54 种缺失场景；有增强的版本多一份 `mask_history.csv`。`terminal.log` 是脚本开始与完成状态，PowerShell 的记录功能未必会收录 Python 子程序逐轮输出；运行时终端会实时打印。最快打开一个例子：

~~~powershell
Invoke-Item 'results\q2_b_clean_1layer_20260924_seed_20260923\terminal.log'
Invoke-Item 'results\q2_b_clean_1layer_20260924_seed_20260923\history.csv'
Invoke-Item 'results\q2_b_clean_1layer_20260924_seed_20260923\summary.json'
~~~

查看 `J` 时记住：它是我们预设的 valid 综合选轮分数，**越低越好**，不是赛题官方指标；正式判断还要同时看 Accuracy、macro-F1、MAE、Pearson 和中性类 F1。任何 smoke 结果都不能与全量模型 A 作成绩比较。四种配置、三个种子的实验顺序和固定参数写在 `results\q2_step3_20260924\实验协议.md`。本步骤训练代码是 `src\q2_train_transformer.py`，窗口运行脚本是 `src\run_q2_transformer.ps1`；两者只读附件二 train/valid 和本机冻结 BERT，不使用附件二 test、附件三或第一问特征。

完整 `-Mode grid` 会在 12 次训练结束后自动生成与模型 A 的对照表。你也可以用下面两行重新生成并打开它。比较程序会先核对每次都是 3395 条 train、728 条 valid、30 轮、同一份附件和 BERT、同一 54 场景；缺一项就报错，不给出“最佳模型”：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step3
Invoke-Item 'results\q2_step3_20260924\candidate_means.csv'
~~~

如果完整训练用了新 `-Tag my_try_01`，比较命令也加 `--tag my_try_01`，新表会放在 `results\q2_step3_my_try_01\`。比较程序仍会用之前固定的模型 A 三种子结果作参照：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step3 --tag my_try_01
Invoke-Item 'results\q2_step3_my_try_01\candidate_means.csv'
~~~

`all_seed_metrics.csv` 能逐个种子审查波动，`candidate_means.csv` 是平均和标准差，`summary.json` 给预设 J 及并列规则挑出的候选。这里只用 valid 选型，不能把它写成最终 test 成绩。第一次失败的空试跑目录 `results\q2_b_smoke_clean_1layer_20260924\` 和后续成功试跑 `results\q2_b_smoke_clean_1layer_20260924b\` 保留原样；前者的 `exit_code.txt=1`，不参与比较。

这次的实际结果和具体判断已整理在 `results\q2_step3_20260924\人工审查结论.md`，场景级直接比较在 `scenario_candidate_means.csv`，P1 独立复验在 `P1独立复验回执.md`。当前最值得先看的三份文件可直接复制打开：

~~~powershell
Invoke-Item 'results\q2_step3_20260924\人工审查结论.md'
Invoke-Item 'results\q2_step3_20260924\candidate_means.csv'
Invoke-Item 'results\q2_step3_20260924\scenario_candidate_means.csv'
~~~

想直接看训练代码，可以复制下面两行用记事本打开，再按 **Ctrl+F** 找 `class GatedTemporalTransformer`（融合和 Transformer）、`def masked_train_view`（训练缺失）、`def evaluate`（54 场景评估）和 `def main`（完整训练流程）。第二行是窗口脚本，核对它是否真的逐轮运行并在前后检查附件：

~~~powershell
notepad 'src\q2_train_transformer.py'
notepad 'src\run_q2_transformer.ps1'
~~~

本步骤完整重跑所需的输入哈希、模型文件、参数和唯一命令在 `results\复现清单.json`；其中也列出作为对照前提的六份模型 A 结果与权重。它现在对应模型 B 四组配置的完整训练与 A/B 比较。只想审查结果时**不用运行它**，因为那会重新训练 12 次。

## 第二问第四步：逐项测试老师的建议

这轮先测两件事：**音频、画面究竟有没有帮上忙**，以及 **Transformer 忽略三模态全缺失位置后有没有改善**。沿用附件二的 train 3395 条、valid 728 条和原来的 54 种验证场景；不碰封存 test、附件三或第一问特征。每次训练 30 轮，终端逐轮显示损失、完整 valid 和缺失 valid 的 Accuracy、F1、MAE、Pearson、J。最终用验证 J 最好的一轮权重，不能只看第 30 轮。

先在可见 PowerShell 中进入本文件第 0 步所示的工程目录。只检查代码逻辑、不读附件的两行是：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_aug_selfcheck
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_train_transformer --selfcheck
~~~

下面先用小样本各试一次，确认新参数能跑通。`T` 是只用文字；`-MaskEmpty` 是让 Transformer 跳过三种模态都不可用的有效位置。每条命令都会显示训练并自动在运行前后核验原始附件：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode smoke -Modalities T -Tag step4_smoke_01
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode smoke -Layers 2 -Augment -MaskEmpty -Tag step4_smoke_01
~~~

正式对照只需逐行粘贴以下四条。每条命令跑三个固定种子、每个种子 30 轮；下一条在上一条窗口结束后再执行。`TA` 是文字加音频，`TV` 是文字加画面；已有 `TAV` 三模态 MLP 结果作为对照，不必重跑。Transformer 只改掩码，其余参数沿用原来的 2 层增强版：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full -Modalities T -Tag step4_20260924
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full -Modalities TA -Tag step4_20260924
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full -Modalities TV -Tag step4_20260924
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode full -Layers 2 -Augment -MaskEmpty -Tag step4_20260924
~~~

结果写在 `results\q2_aug_T_step4_20260924_seed_20260923\`、`results\q2_aug_TA_...\`、`results\q2_aug_TV_...\` 和 `results\q2_b_aug_2layer_step4_20260924_seed_20260923\` 等目录；后两个种子把末尾改为 `20260924`、`20260925`。最佳权重在 `checkpoints\` 下同名目录。最容易看懂的是 `summary.json` 中 `best.clean`（完整样本）和 `best.masked_mean`（54 种缺失平均）；`history.csv` 看各轮变化，`scenario_metrics.csv` 看具体缺失情况，`terminal.log` 看脚本起止，`exit_code.txt` 为 `0` 才算正常完成。例如：

~~~powershell
Invoke-Item 'results\q2_aug_T_step4_20260924_seed_20260923\summary.json'
Invoke-Item 'results\q2_aug_T_step4_20260924_seed_20260923\history.csv'
Invoke-Item 'results\q2_b_aug_2layer_step4_20260924_seed_20260923\summary.json'
~~~

比较时把相同种子放在一起看：`T` 对 `TA` 判断声音帮助，`T` 对 `TV` 判断画面帮助，再和已有 `TAV` 比较；新 Transformer 对旧 `q2_b_aug_2layer_20260924_seed_...` 判断仅修掩码的效果。F1/Accuracy/Pearson 越高越好，MAE/J 越低越好。`T` 模型遇到“只遮音频或画面”的场景，输入本来就不会变化，这种场景不能解释成它对缺失音视频有特殊抵抗力。若输出目录已存在，运行脚本会阻止覆盖；重跑时改 `-Tag`。

等四条正式命令全跑完，用下面两行汇总全部 18 份同种子结果。程序会检查 30 轮、54 场景、输入哈希、模型配置和退出码，缺失或不一致时直接报错：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step4 --tag step4_20260924
Invoke-Item 'results\q2_step4_20260924\candidate_means.csv'
~~~

同目录的 `all_seed_metrics.csv` 看每个种子的真实值，`paired_deltas.csv` 看同种子相减，`missing_group_means.csv` 单独看“文字缺失”和具体缺失模态时的表现。对 `delta_*` 列，F1/Accuracy/Pearson 为正才改善，MAE/J 为负才改善。比较程序只读已有结果和权重路径，不重新训练；默认输出目录已存在时会停止，以免覆盖旧比较。若训练改用了新 `-Tag`，这里的 `--tag` 要填同一个值。

## 第二问第五步：试老师建议的可靠性门控

前一步若发现音频和画面的作用不稳定，可继续试这个小模型。它仍使用**同一份附件二 aligned 数据、同一连续缺失训练方式和同一 54 种验证场景**；区别是先分别处理文字、声音、画面的片段特征，再参考三者各自的有效位置比例，为每条样本分配融合权重。此轮只改融合方式，不加入新的损失或更大的时序模型。它是对“可靠性门控有没有用”的测试，不等于老师整套方案已经完成。

先在可见 PowerShell 中复制这两行做逻辑自检和一轮小样本试跑：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_aug_selfcheck
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode smoke -Fusion coverage_gate -Tag step5_smoke_01
~~~

试跑通过后，下面这一行会依次训练三个种子，每次 30 轮；窗口逐轮显示损失和完整/缺失 valid 指标，自动在每次训练前后核验原始附件：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full -Fusion coverage_gate -Tag step5_20260924
~~~

结果在 `results\q2_aug_gate_step5_20260924_seed_20260923\` 等三个目录，权重在 `checkpoints\` 下同名目录。每个结果必须有 `exit_code.txt=0`、30 行 `history.csv`、54 行 `scenario_metrics.csv`，并且训练前后原始核验为 PASS。最简单的人工检查：

~~~powershell
Invoke-Item 'results\q2_aug_gate_step5_20260924_seed_20260923\summary.json'
Invoke-Item 'results\q2_aug_gate_step5_20260924_seed_20260923\history.csv'
Invoke-Item 'results\q2_aug_gate_step5_20260924_seed_20260923\terminal.log'
~~~

三个种子全部成功后，复制下面两行把门控模型与第四步的结果、原三模态 MLP 放在同一张表。汇总程序只读日志，不重新训练；新报告在 `results\q2_step5_20260924\`。如果你换了训练 `-Tag`，记得改 `--gate-tag`：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step4 --tag step4_20260924 --gate-tag step5_20260924
Invoke-Item 'results\q2_step5_20260924\candidate_means.csv'
~~~

重点看 `paired_deltas.csv` 中 `A_gate` 对 `A_TAV`、`A_T` 的同种子差值，以及 `missing_group_means.csv` 的 `text_missing` 行。门控模型若只在总平均略有改善、而文字缺失时没有改善，就不能称它解决了局部缺失问题。`J` 仍只是本项目的选轮分数，不是 Accuracy；正式看三分类 F1/Accuracy 和强度 MAE/Pearson。

这一轮已经完成的简明结论可直接打开，不需要重新运行训练：

~~~powershell
Invoke-Item 'results\q2_step5_20260924\人工审查结论.md'
~~~

## 第二问第六步：不做额外遮蔽的模态对照

第四步的增强训练会随机选被遮住的模态；“仅文本”碰到只遮音频/画面时，增强输入实际上和原输入相同。为单独判断模态特征是否有用，这里**训练时不额外制造缺失**，只改模型可见的模态；验证时仍统一跑 54 个缺失场景。已有的原三模态无增强 MLP 三种子结果作为对照，不重跑。

复制这行做 128 条 train/64 条 valid 的一轮试跑，确认 `-Clean` 开关正常：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode smoke -Clean -Modalities T -Tag step6_smoke_01
~~~

试跑通过后，下面三行依次训练完整的仅文本、文本＋音频、文本＋视觉模型，各三个种子、各 30 轮。每轮在可见 PowerShell 中打印损失和验证效果，训练前后自动核验原始附件：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full -Clean -Modalities T -Tag step6_20260924
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full -Clean -Modalities TA -Tag step6_20260924
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full -Clean -Modalities TV -Tag step6_20260924
~~~

输出在 `results\q2_clean_T_step6_20260924_seed_20260923\` 等目录，权重在 `checkpoints\` 下同名目录。确认各次 `exit_code.txt=0`、`history.csv` 有 30 轮、`scenario_metrics.csv` 有 54 行后，复制以下命令做完整汇总：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step4 --tag step4_20260924 --gate-tag step5_20260924 --clean-tag step6_20260924
Invoke-Item 'results\q2_step6_20260924\candidate_means.csv'
Invoke-Item 'results\q2_step6_20260924\paired_deltas.csv'
~~~

表中名称以 `_clean` 结尾的四行才用于比较“加音频/视觉本身”的价值；没有该后缀的是之前连续缺失增强的结果。`paired_deltas.csv` 中的 `A_TA_clean`、`A_TV_clean`、`A_TAV_clean` 都是与同种子的 `A_T_clean` 相减。现成结果显示：无增强训练中，TV 的 J 三个种子都比 T 低，TA 三个种子都比 T 高。看表无需重新训练；完整结论可以直接打开：

~~~powershell
Invoke-Item 'results\q2_step6_20260924\人工审查结论.md'
~~~

## 第二问第七步：确认全零行等价，并修正无效遮蔽

此前训练的 TV 模型只用文字和画面；若随机抽到“只遮音频”，增强样本的输入其实没有变化。本步只改训练遮蔽的抽法：从模型实际使用的模态里选，并确认选中的连续段原本至少有一行有效特征。长度仍为有效位置的 10%、30% 或 50%；旧结果和代码默认行为均保留。

先在工程目录复制下面四行。第一行不读附件；中间两次完整性检查夹住“音视频整行置零”和“内部缺失掩码”的等价试验。试验从附件二 valid 只读 16 条样本，不训练模型，也不读取附件三。第三行成功时会报告 A、V、AV 三种遮蔽下均值输入与时序模型预测的最大差异，要求不超过 `1e-6`：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_aug_selfcheck
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_input_equivalence --tag step7_20260924
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

等价试验的结果在 `results\q2_step7_input_equivalence_step7_20260924.json`。文件不存在或任一命令退出码非 0 时，先查报错，不进入正式训练。`--tag` 只是输出名；重跑改一个新值，脚本不会覆盖旧结果。

再用下面一行做一轮小样本试跑；会打开可见终端打印损失和验证效果，前后核验原始附件：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode smoke -Modalities TV -EffectiveMask -Tag step7_smoke_20260924
~~~

小样本通过后，复制下面一行跑三个固定种子，每个种子完整 30 轮；这与旧 TV 增强版的模型和验证场景相同，只改遮蔽抽法：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_augmented.ps1' -Mode full -Modalities TV -EffectiveMask -Tag step7_20260924
~~~

新结果依次写到 `results\q2_aug_TV_eff_step7_20260924_seed_20260923\`、`...20260924\`、`...20260925\`，权重在 `checkpoints\` 同名目录。先看每个目录的 `exit_code.txt` 是否为 `0`，再打开 `mask_history.csv` 检查 `group_A`、`group_TA`、`group_AV` 均为 `0`，`effective_rows` 加 `fallback_clean` 等于训练样本数。`history.csv` 看 30 轮的损失和指标，`scenario_metrics.csv` 看 54 种缺失；最佳权重按 valid 的 J 选轮，J 不是 Accuracy。第四至第六步的旧训练结果不能因文件名相似被覆盖。

三个完整种子都成功后，这行只读取已有结果并生成新旧 TV 的配对比较，不重新训练：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step8 --stage tv --tv-tag step7_20260924 --output-tag 20260924
Invoke-Item 'results\q2_step8_compare_tv_20260924\paired_deltas.csv'
~~~

## 第二问第八步：分别读取三种模态时间顺序的主模型

这是老师建议的核心结构试验。文字、音频、画面各用一条小型时序编码分支，再汇总有效位置；`concat` 直接合并，`reliability_gate` 再按每条样本的模态可用性分配权重。两版都做负／中／正三分类和 `[-3,3]` 强度预测。事先固定的模型、训练预算与比较规则在 `results\q2_step8_20260924\实验协议.md`；输入仍只来自附件二 train/valid，附件三不参与。

先做不读附件的结构自检，然后各用 128 条 train、64 条 valid 跑一轮小样本。试跑只说明链路正常，不是成绩。下面每条都会在**可见 PowerShell 窗口**打印损失与验证指标，且在读原始附件前后做完整性核验：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_train_transformer --selfcheck
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode smoke -Architecture separate -Fusion concat -Layers 1 -Augment -EffectiveMask -Tag step8_concat_smoke_20260924
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode smoke -Architecture separate -Fusion reliability_gate -Layers 1 -Augment -EffectiveMask -Tag step8_gate_smoke_20260924
~~~

两次试跑成功后，依次运行下面两条正式命令。每条训练三个种子、每种子 30 轮，逐轮打印完整 valid 和 54 种缺失 valid 的 Accuracy、F1、MAE、Pearson、J。第一条先判断“分模态时序编码”有没有价值，第二条只加可靠性门控：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode full -Architecture separate -Fusion concat -Layers 1 -Augment -EffectiveMask -Tag step8_20260924
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode full -Architecture separate -Fusion reliability_gate -Layers 1 -Augment -EffectiveMask -Tag step8_20260924
~~~

输出在 `results\q2_c_aug_1layer_concat_eff_step8_20260924_seed_20260923\` 等三个 concat 目录和 `q2_c_aug_1layer_gate_eff_step8_20260924_seed_20260923\` 等三个门控目录；权重在 `checkpoints\` 同名目录。每个结果先看 `exit_code.txt=0`，再看 `history.csv` 有 30 轮、`scenario_metrics.csv` 有 54 行、`mask_history.csv` 的 `effective_rows` 与 `fallback_clean`。`summary.json` 记录最佳轮、结构、参数量和完整运行命令。文件已存在时脚本会停止；重跑改 `-Tag`，不要覆盖旧结果。

**人工比较**：先看同种子的 concat 与 gate，再和第三步早融合 Transformer、第四／六步 MLP 比。不能只看 J；缺失 F1 越高、MAE 越低越好，还要看完整输入的 Accuracy/F1 有没有退步。音频在旧均值池化 MLP 上无收益，不代表这条新时序音频分支无用。两种配置都用相同的 54 场景，所以可以逐场景比较。模型训练代码在 `src\q2_train_transformer.py`，窗口脚本在 `src\run_q2_transformer.ps1`；两者的新增选项只影响标注为 `q2_c_...` 的新结果。

六次新模型训练均成功后，复制下面两行汇总三种子均值和同种子差值；它们只读已有日志，若缺少任一次训练、哈希或场景不一致就报错：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step8 --stage all --tv-tag step7_20260924 --model-tag step8_20260924 --output-tag 20260924
Invoke-Item 'results\q2_step8_compare_all_20260924\candidate_means.csv'
Invoke-Item 'results\q2_step8_compare_all_20260924\paired_deltas.csv'
Invoke-Item 'results\q2_step8_compare_all_20260924\missing_group_means.csv'
~~~

**已跑出的第八步结果**：六次训练均跑满 30 轮，三种子平均 J（越低越好）为直接拼接 `0.6286`、可靠性门控 `0.6334`；之前的 TV 均值池化旧增强版为 `0.6097`。打开 `results\q2_step8_20260924\人工审查结论.md` 看完整指标和限制。

## 第二问第九步：只降低一次学习率，排查过早回落

第八步两种 Transformer 的最佳轮次都在第 3～4 轮，之后训练损失继续下降、验证变差。本步只把**直接拼接版**学习率从 `0.001` 改为 `0.0003`，其余训练不动；事先写定的规则在 `results\q2_step9_20260924\实验协议.md`。这仍只用附件二 train/valid。

先复制这一行做一轮小样本试跑。它会打开可见 PowerShell 窗口，打印损失与验证指标，并在前后检查原始附件：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode smoke -Architecture separate -Fusion concat -Layers 1 -Augment -EffectiveMask -LearningRate 0.0003 -Tag step9_lr3e4_smoke_20260924
~~~

试跑退出码为 0 后，复制这一行跑三个种子、每个 30 轮：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode full -Architecture separate -Fusion concat -Layers 1 -Augment -EffectiveMask -LearningRate 0.0003 -Tag step9_lr3e4_20260924
~~~

结果依次在 `results\q2_c_aug_1layer_concat_eff_step9_lr3e4_20260924_seed_20260923\` 等三个目录，最佳权重在 `checkpoints\` 同名目录。先看 `exit_code.txt` 是否为 `0`，再看 `history.csv` 是否有 30 轮、`scenario_metrics.csv` 是否有 54 行；训练前后原始核验的实际证据由核验程序写到 `results\codex_review\raw_integrity_*.json`，并在运行终端显示 `PASS`。`summary.json` 的 `learning_rate` 应为 `0.0003`，`best_epoch` 是选出的最佳轮次。想重跑要换新 `-Tag`，不能覆盖旧结果。

三次都成功后复制下面两行，只读取已有结果，与旧学习率和 TV 基线做同种子比较：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step8 --stage lr --model-tag step8_20260924 --lr-tag step9_lr3e4_20260924 --output-tag 20260924
Invoke-Item 'results\q2_step9_compare_lr_20260924\candidate_means.csv'
Invoke-Item 'results\q2_step9_compare_lr_20260924\paired_deltas.csv'
~~~

比较表中 `C_concat_lr` 是新学习率，`C_concat` 是旧学习率，`TV_old` 是此前最好的文字＋画面均值池化基线。J 越低越好；还要看完整和缺失条件下的 F1 是否更高、MAE 是否更低。当前所有模型都按同一 valid 选轮，多次比较属于探索，不能当最终成绩。

**已跑出的第九步结果**：三个低学习率种子均跑满 30 轮，平均 J 从原设置的 `0.6286` 降到 `0.6141`，TV 旧增强版为 `0.6097`。详细看 `results\q2_step9_20260924\人工审查结论.md`。

## 第二问第十步：只加完整／遮蔽预测一致性

这一步固定第九步的模型与 `0.0003` 学习率，只在两个训练视图的监督损失外，增加一项权重为 `0.5` 的预测一致性约束。该约束让遮蔽后的三分类概率和情感强度不要无理由偏离完整输入；完整输入在这项约束里作为停止梯度的参照。训练前写定的公式和判断规则在 `results\q2_step10_20260924\实验协议.md`。不同时加课程训练或其他一致性项。

先复制这一行试跑一次，看到退出码 `0` 后再跑三个完整种子；两条都会打开可见终端，逐轮打印总损失、分类损失、回归损失、一致性损失，以及完整和缺失验证指标，并在前后核验原始附件：

~~~powershell
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode smoke -Architecture separate -Fusion concat -Layers 1 -Augment -EffectiveMask -LearningRate 0.0003 -ConsistencyWeight 0.5 -Tag step10_cons05_smoke_20260924
powershell.exe -NoExit -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode full -Architecture separate -Fusion concat -Layers 1 -Augment -EffectiveMask -LearningRate 0.0003 -ConsistencyWeight 0.5 -Tag step10_cons05_20260924
~~~

结果在 `results\q2_c_aug_1layer_concat_eff_step10_cons05_20260924_seed_20260923\` 等三个目录，权重在 `checkpoints\` 同名目录。检查方法同第九步：`exit_code.txt=0`、`history.csv` 有 30 行、`scenario_metrics.csv` 有 54 行；原始核验看 `results\codex_review\raw_integrity_*.json` 和运行终端的 `PASS`。`summary.json` 还应记载 `learning_rate=0.0003` 和 `consistency_weight=0.5`。每轮 `history.csv` 的 `train_consistency_loss` 是未乘 `0.5` 的约束值；总损失已经乘入权重。重跑时改 `-Tag`，不要覆盖旧结果。

三次训练都成功后，只读已有结果做配对比较：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_step8 --stage consistency --lr-tag step9_lr3e4_20260924 --cons-tag step10_cons05_20260924 --output-tag 20260924
Invoke-Item 'results\q2_step10_compare_consistency_20260924\candidate_means.csv'
Invoke-Item 'results\q2_step10_compare_consistency_20260924\paired_deltas.csv'
Invoke-Item 'results\q2_step10_compare_consistency_20260924\missing_group_means.csv'
~~~

`C_concat_cons` 是新模型，`C_concat_lr` 是只降低学习率的对照，`TV_old` 是当前综合 J 最好的 MLP。先看同种子差值是否方向一致，再看 F1 和 MAE 是否同时合理；只有一项指标变好不算整体改善。当前所有实验都是探索性 valid 比较。

**已跑出的第十步结果**：三次一致性训练均跑满 30 轮，平均 J 从 `0.61408` 到 `0.61389`，变化仅 `0.00020`，且三个种子方向不一致；缺失 F1 基本相同、MAE 略差。按预先规则，此设置不作为优先模型，也不继续堆叠更多训练技巧。详细证据见 `results\q2_step10_20260924\人工审查结论.md`。

## 第二问验证集审查：原有全零行和新增遮蔽分开看

这一步**只读取附件二 valid 和已经训练好的权重，不训练、不使用封存 test 或附件三**。它把 728 段 valid 分成“音频／视觉有效词位置已有全零行”和“没有这类全零行”，分别计算 TV 旧增强 MLP 与低学习率时序 Transformer 的完整、54 种遮蔽平均指标。同时逐场景统计真正新遮掉的可观测行，检查名义上的 10%、30%、50% 遮蔽有没有落在本来就是零的位置。结果还会和原训练日志逐项核对，差异过大就报错，不写通过。

复制下面三行到 PowerShell，顺序执行。前后两行核验原始附件，中间一行是只读评估；每行应正常结束，第三行再次显示 `PASS`：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_valid_missing_audit --tag 20260924_v2
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

审查时直接打开下面三张表；`cohort_means.csv` 看原本有／无全零行的模型成绩，`loss_group_means.csv` 看六类缺失总体真正生效多少，`scene_actual_loss.csv` 看每个模态组、位置、遮蔽长度和样本分组的详细数字。比较遮蔽比例时，用同分母的 `nominal_weighted_ratio` 和 `actual_newly_lost_content_ratio`；`effective_target_fraction` 表示打算遮的行里有多少原本确实可观测。`tv_no_new_loss_clips_per_scene` 显示对只用文字与画面的 TV 模型有多少样本完全没有新增缺失。程序还检查音频单独遮蔽时 TV 模型输入逐位相同，并允许 GPU 以不同批量计算预测时小于 `1e-5` 的浮点差异。`summary.json` 记录原始零行数量、输入哈希和与旧日志的最大复现误差。零行只表示**疑似无观测**，不自动判为主办方刻意制造的缺失。最初的 `20260924` 目录保留作过程记录，人工审查以修正后的 `20260924_v2` 为准。

~~~powershell
Invoke-Item 'results\q2_valid_missing_audit_20260924_v2\cohort_means.csv'
Invoke-Item 'results\q2_valid_missing_audit_20260924_v2\loss_group_means.csv'
Invoke-Item 'results\q2_valid_missing_audit_20260924_v2\scene_actual_loss.csv'
Invoke-Item 'results\q2_valid_missing_audit_20260924_v2\summary.json'
~~~

程序在 `src\q2_valid_missing_audit.py`。如果同名结果已经存在，改用一个新的 `--tag`，并打开对应的新目录；程序不会覆盖旧审查结果。两类样本数量不同，直接比较各自 F1 时还要看 `cohort_seed_metrics.csv` 的三类标签数量。

## 第二问：看一次封存的附件二 test 成绩

前面的模型只用 train 训练、valid 挑选。现在先固定文本＋画面 MLP 的三个种子作为主模型，把三模态低学习率 Transformer 的三个种子作为固定对照。`results\q2_locked_test_20260924\模型锁定与测试协议.md` 是**看 test 成绩前写好的规则**。这一步不训练，也不拿 test 改模型。需要本机原始附件二、D 盘已有的 BERT 和保存的六份权重；不使用第一问的特征或附件三。

你只想看结果时，直接运行下面三行，**不要重复运行评估程序**。第一行核验原始附件，中间一行评估 727 条 test 和 54 种连续遮蔽，最后一行再核验附件。每行执行完应无报错；前后两次应显示 `PASS`。本程序完成后发现同名结果会拒绝重跑，避免改写首次 test 成绩：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_locked_test_eval
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

完成后打开主表和场景表：

~~~powershell
Invoke-Item 'results\q2_locked_test_20260924\test_model_metrics.csv'
Invoke-Item 'results\q2_locked_test_20260924\test_scenario_metrics.csv'
Invoke-Item 'results\q2_locked_test_20260924\test_summary.json'
~~~

主表中 `TV_primary` 是事先固定的主模型，`C_fixed_comparator` 是固定对照；先看 `seed=ensemble` 且 `cohort=all` 的两行。`clean_...` 表示不加人为缺失，`masked_...` 是 54 个场景的平均，`J_descriptive` 越低越好。Accuracy、F1、Pearson 越高越好，MAE 越低越好。`natural_zero` 表示原数据有效词位置已有音频或视觉全零行，**不证明主办方人为制造缺失**。两组样本数不同，先看 `n`，不要只比较两个组的 F1。场景表每个模型 54 行，可查某一模态、位置和遮蔽长度；同一个 test 样本在各场景重复出现，54 行不能当作 54 次独立测试。`src\q2_locked_test_eval.py` 是本步骤的代码，输出只在 `results\q2_locked_test_20260924\`。test 结果用于报告泛化，不能据此换主模型、调参数或挑种子。

**这次已经跑过。**最快看结论是打开 `results\q2_locked_test_20260924\独立测试结果.md`。如要在另一份工程上首次运行，才使用上面的三行；同目录已有结果时程序会拒绝覆盖。

## 第二问：查看附件三 30 条预测

附件三的对齐版每条只有 `text_bert`、声音和画面特征，**没有情感标签，也没有现成的 768 维文字特征**。程序用本机 D 盘已固定版本的 BERT 从 token ID 重建文字特征。默认 `--model TV` 重现先前锁定的文字＋画面预测；`--model C` 则调用已经训练好的三模态分支 Transformer，真正读取文字、声音、画面。两种模型都输出负／中／正及 `[-3,3]` 强度，不重新训练或改权重。原始文件、已保存权重和 BERT 都只读。需要附件三 30 个对齐版 pkl、D 盘 BERT 及对应模型的三份权重。

想看现成结果，直接执行：

~~~powershell
Invoke-Item 'results\q2_attachment3_locked_20260924\predictions.csv'
Invoke-Item 'results\q2_attachment3_locked_20260924\summary.json'
Invoke-Item 'results\q2_attachment3_locked_20260924\人工审查说明.md'
Invoke-Item 'results\q2_attachment3_three_modal_20260924\predictions.csv'
~~~

若是首次在另一份工程运行，按顺序复制下面三行；前后两次原始附件核验应显示 `PASS`，中间程序结束后应显示 30 条样本。**已有同名预测时程序会停止，不会覆盖第一次结果**：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_predict_attachment3 --model C
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

上面中间一行使用三模态 C 版，输出到 `results\q2_attachment3_three_modal_20260924\`，不会覆盖旧 TV 文件。想在全新工程复现旧 TV，则把 `--model C` 改成 `--model TV`，输出到 `results\q2_attachment3_locked_20260924\`。如果同名预测已存在，程序会拒绝覆盖。

在 `predictions.csv` 中，每行一条，`sample_id` 只是本地文件名，不是假装存在的官方 ID；`polarity_code` 的 `0/1/2` 分别是 negative/neutral/positive；`p_negative/p_neutral/p_positive` 三项相加约为 1；`strength` 在 -3 到 3。`audio_zero_rows` 和 `vision_zero_rows` 是有效词位置上原本已全零的行数，`...longest_zero_block` 是最长连续段，供你筛查困难样本。概率是模型输出分数，**没有做概率校准**；附件三没有真值，所以不能由这张表算 Accuracy、F1、MAE 或 Pearson。`input_manifest.csv` 记录每个原文件的 SHA-256 以便复现，`summary.json` 看预测数量和类别分布。推理代码为 `src\q2_predict_attachment3.py`。这张表是供人工审查的明细，正式提交列名应再按题面核对。

**已经打包好的第二问三模态交付件**在 `submission\Q2_三模态交付_20260924.zip`（约 3.98 MB，31 个文件），同名文件夹可直接打开。包含 30 行正式列名预测 CSV、三份模型权重、核心代码、验证／测试表和模型角色修正说明；原始大数据与 440 MB BERT 不在包内。直接看预测或交付说明可复制：

~~~powershell
Invoke-Item 'submission\Q2_三模态交付_20260924\附件3_三模态预测.csv'
Invoke-Item 'submission\Q2_三模态交付_20260924\README_交付说明.md'
Invoke-Item 'submission\Q2_三模态交付_20260924.zip'
~~~

这个压缩包只是**第二问组件**，还没有把第一、三问及最终论文合在一起；完整参赛提交前仍要做总容量与匿名核验。本机复查直接打开现成表，不需要重训。`--model C` 需要在原工程执行，现有同名结果会阻止覆盖；在干净的工程副本中才可重新推理。

## 真要从原视频重算第一问时

日常人工审查**不用执行本节**。下面会覆盖同名派生文件；原视频、原始附件和标签表保持只读。重算前先保存你填写过的人工表和现有特征：

~~~powershell
Copy-Item 'results\q1_v2\q1_manual_review_sheet.csv' ('results\q1_v2\q1_manual_review_sheet_backup_' + (Get-Date -Format 'yyyyMMdd_HHmmss') + '.csv')
Compress-Archive -Path 'data\processed\q1','data\processed\q1_v2' -DestinationPath ('results\q1_features_backup_' + (Get-Date -Format 'yyyyMMdd_HHmmss') + '.zip')
~~~

需要原始 100 段 MP4、label-100.xlsx 和本机 D 盘已放好的 BERT、音频、人脸模型及 Python 环境。下面是真正重算全部 100 段的顺序；原始完整性检查在前后都要跑。不要加 --resume，否则已有的 JSON/NPZ 会被跳过：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_extract --fps 10
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_validate
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_visual_refine
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_validate --variant q1_v2
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_quality_dashboard --variant q1_v2
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_compare_versions
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

旧版特征在 data\processed\q1\，新版在 data\processed\q1_v2\，比较结果在 results\q1_v2\q1_version_comparison.csv/.json 和 q1_face_review_priority.csv。新版仅对旧版人脸覆盖率较低的片段试 70%/50% 居中裁剪；文字、声音和词时间沿用旧版。提取清单是 results\q1_feature_inventory.csv 和 results\q1_v2\q1_feature_inventory.csv。想先试少量视频，q1_extract 可加 --limit 3，q1_visual_refine 可加 --limit 3；这种部分结果**不代表全量 100 段已经复现**。要指定 ID，可用单引号写 --ids=ID1,ID2。q1_extract 的 --fps 0 会处理全部视频帧，耗时更大。

如果需要**重新生成空白人工复核表**，先确认旧表已另存，再运行：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.q1_prepare_manual_review --variant q1_v2
~~~

它读取 results\q1_v2\q1_review_queue.csv，输出 results\q1_v2\q1_manual_review_sheet.csv，**会覆盖同名表中已有的人工填写**；看表或继续填写不需要运行它。

## 遇到问题时先看这里

- PowerShell 显示“找不到路径”：先重新执行第 0 步，并用 Test-Path 检查该文件是否真的存在。
- 显示缺少原视频：四层图和人脸试验需要原 MP4；已有热图、统计图和表格不需要。第一问 JSON 中的 video_path 是原电脑记录的路径。
- 显示缺少模型或哈希不符：第二问训练要求附件二和本机冻结 BERT 权重完整；第一问重提取还要音频、人脸等模型。不要修改原始附件来绕过检查。
- 样本 ID 含 $：把整个 --sample-id=... 或 --ids=... 参数放在单引号内。
- 自动 PASS、较高人脸检出率和漂亮的热图，都不能代替播放视频、听原声、确认目标人脸。

## 第二问今晚新增实验：我怎样看、怎样重新跑

**想审查结果，不需要重新训练。**打开 PowerShell，复制下面两行：

~~~powershell
Set-Location -LiteralPath 'D:\第二十三届中国研究生数学建模竞赛 - 中文题目\中文题目\E题\工程'
Invoke-Item 'results\q2_tonight_20260924\candidate_means.csv'
~~~

表中 `C_current` 是原三模态模型；`C_gate` 是同学习率的门控；`C_lr1e4` 只降学习率；`C_uni` 加三个单模态辅助头；`C_gate_uni` 是门控加辅助头；`C_multi` 是一／两个连续缺失段增强。`mean_selection_J` **越小越好**，`mean_clean_macro_f1`、`mean_masked_macro_f1` 越大越好，`mean_masked_mae` 越小越好。`selection.json` 写明按训练前规则选出的候选；`paired_deltas.csv` 列出每个种子相对原模型的变化。先看三种子平均，再看三个种子方向是否一致；单个种子好不算稳定提分。每个训练目录里的 `history.csv` 有 30 轮损失和验证成绩，`terminal.log` 记录脚本开始和结束状态，`exit_code.txt` 应为 `0`。Python 逐轮输出在运行终端实时显示，但 Windows PowerShell 的脚本记录未必把子程序输出写入 `terminal.log`；复看逐轮数字请打开 `history.csv`。

**只在新的工程副本且这些同名结果不存在时**，依次复制下面三行，才会真正重训。需要本机附件二、D 盘 BERT、已有 `..\训练demo\.venv` 环境和 GPU；训练会逐轮在终端打印完整／缺失验证成绩，总计多个三种子实验，耗时明显长于单次模型训练：

~~~powershell
Set-Location -LiteralPath 'D:\第二十三届中国研究生数学建模竞赛 - 中文题目\中文题目\E题\工程'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\src\run_q2_transformer.ps1' -Mode full -Architecture separate -Fusion reliability_gate -Layers 1 -Augment -EffectiveMask -LearningRate 0.0003 -Tag tonight_gate_20260924
powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\src\run_q2_tonight.ps1'
~~~

第一条训练三种子门控；第二条先自检新增功能，再短跑三次检查，然后顺序完成其余四组三种子训练，最后比较验证成绩。每次训练前后会检查原始附件完整性；任一步失败应停在该步，查看对应目录的 `terminal.log`、`exit_code.txt`，不要把缺少的结果填成通过。想单独复现一个候选，可打开 `src\run_q2_tonight.ps1` 复制对应那一行，修改 `-Tag` 保存到新目录。训练器 `src\q2_train_transformer.py` 的新增参数是 `-AuxiliaryWeight 0.1`（单模态辅助监督）和 `-MaskPattern multi`（一或两个分离缺失段）；后者的抽样器在 `src\q2_missing_patterns.py`，可先运行 `& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_missing_patterns`，应显示 `PASS`。

这些对照都只用附件二 train/valid；附件二 test 已经查看过，不能再拿它选新模型。附件三无标签，最终预测只能在确定模型后运行，不能由它计算准确率。

想直接审查今晚全部结果，再复制下面几行。`实验协议.md` 是训练前写好的比较规则；`人工审查结论.md` 用通俗文字汇总真实结果。`neutral_confusion.csv` 有 3×3 的真实类别与预测类别人数，可重点看中性被错判成负面或正面的数量：

~~~powershell
Invoke-Item 'results\q2_tonight_20260924\实验协议.md'
Invoke-Item 'results\q2_tonight_20260924\人工审查结论.md'
Invoke-Item 'results\q2_tonight_20260924\paired_deltas.csv'
Invoke-Item 'results\q2_tonight_20260924\missing_group_means.csv'
Invoke-Item 'results\q2_tonight_20260924\neutral_confusion.csv'
~~~

`src\q2_compare_tonight.py` 只读训练日志，把六种方案的三种子成绩并排列出来，并按固定规则写 `selection.json`；训练完成前不要运行。`src\q2_neutral_diagnostic.py` 只用附件二 valid 和已有权重重算中性类误判，需要原始附件和权重，单独复查时复制下面三行，前后都应显示原始文件 `PASS`；同名诊断结果已存在时程序会拒绝覆盖，请直接打开已有表：

~~~powershell
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_neutral_diagnostic
& '..\训练demo\.venv\Scripts\python.exe' -m src.tools.verify_raw_integrity
~~~

选定模型之后，`src\q2_predict_attachment3.py --model C --candidate-prefix <训练目录公共前缀>` 才把三份三模态权重用于附件三 30 条无标签样本。参数应填 `selection.json` 对应的实际训练目录前缀，去掉最后的 `_seed_20260923`；它会新建独立预测目录，保留旧预测。如果选择 `C_current`，现成预测已经在 `results\q2_attachment3_three_modal_20260924\`，直接打开即可，无需再次推理。新候选的命令和最终预测路径会写在今晚的 `人工审查结论.md`。

`src\q2_package_tonight.py` 只在预测和人工审查结论齐全后运行；它读取现成结果、代码和三份权重，生成 `submission\Q2_今晚交付_20260924.zip`，并核对压缩包 CRC 与 50 MiB 限额。它不重新训练；同名包已有时拒绝覆盖。想直接看交付内容，运行：

~~~powershell
Invoke-Item 'submission\Q2_今晚交付_20260924\README_交付说明.md'
Invoke-Item 'submission\Q2_今晚交付_20260924\附件3_三模态预测.csv'
~~~

包里不含原始数据和大型 BERT 模型，因此不能只靠 ZIP 在全新电脑上重训。附件三无情感真值，预测文件中的概率不能当作已验证的准确率。

**今晚实际结论：使用 `C_current`，学习率 `3e-4`。**五个新对照都没有达到训练前固定的稳定提升规则；把学习率降为 `1e-4` 的三种子平均 J 从 `0.61408` 变成 `0.61959`，越低越好。详细表格、单种子变化、门控的 MAE 与 F1 取舍，以及中性类错判人数，都在 `results\q2_tonight_20260924\人工审查结论.md`。由于最终选回现有三模态版本，附件三直接沿用 `results\q2_attachment3_three_modal_20260924\predictions.csv`，无需重复推理。

在另一个有相同原始附件、模型和训练结果的工程副本里首次生成汇总表时，确认六组各三个训练结果都完整，再运行 `& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_compare_tonight`；它只读日志，输出到 `results\q2_tonight_20260924\`。同名表已存在时会拒绝覆盖。若要首次打包且 `人工审查结论.md`、附件三预测和三份最终权重都已齐全，完整命令是：

~~~powershell
Set-Location -LiteralPath 'D:\第二十三届中国研究生数学建模竞赛 - 中文题目\中文题目\E题\工程'
& '..\训练demo\.venv\Scripts\python.exe' -m src.q2_package_tonight
~~~

输出为 `submission\Q2_今晚交付_20260924\` 和同名 ZIP；程序会验证附件三正好 30 行、模型训练退出码、ZIP CRC 和 50 MiB 限额。**本机已有同名包后，只打开文件审查，不要再运行打包命令**。这里的 ZIP 是第二问组件，正式交赛前仍须与第一、三问和论文合并核对。
