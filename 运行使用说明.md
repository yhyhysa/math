# E 题工程：可运行文件使用说明

本文件是工程中可运行的解题程序、核验程序和可视化 Demo 的统一使用入口。以后新增或改动这类程序时，在**本文件**同步更新依赖、输入、完整命令、参数、输出和读图方法。命令均从 `E题/工程` 目录执行；PowerShell 中带 `$` 的样本 ID 请用**单引号**包住。

## 先确认自己能运行哪一层

| 条件 | 可运行内容 |
|---|---|
| 只克隆 GitHub 仓库 | Q1 已有特征的结构核验、统计图、三模态时间热图、人工复核表。仓库含 `data/processed/q1/` 的 100 个 JSON 和 100 个 NPZ。 |
| 另有题目原始视频，且现有 JSON 的 `video_path` 指向可读取的视频 | 四层审查图（含原始画面和音频）。若换电脑，先将 JSON 中的本机绝对路径映射到实际视频路径，或调整脚本的读视频逻辑。 |
| 再有 MediaPipe 模型及提取环境 | 人脸阈值与画面裁剪试验。 |
| 再有 BERT、Wav2Vec2、openSMILE 等完整模型与依赖 | 从原始视频重新提取 Q1 特征。 |

本机已有完整 Python：`..\训练demo\.venv\Scripts\python.exe`。队友只检查仓库结果时，可在 `工程` 内建立 Python 3.12 环境并执行：

```powershell
py -3.12 -m venv .venv
& .\.venv\Scripts\python.exe -m pip install -r requirements-review.txt
```

下文的 `& ..\训练demo\.venv\Scripts\python.exe` 可换成 `& .\.venv\Scripts\python.exe`。四层审查图还需要 `av`；人脸试验还需要 `av`、`mediapipe` 和 `models/face_landmarker.task`。这些原始视频及模型**不在 Git 仓库里**。完整提取环境见 `README.md` 和 `TEAMMATE_GUIDE.md`。

## 已有结果与统计口径

当前 Q1 已处理 100 段原始视频：解码 23,241 帧，在真实视频时间戳上约以 10 帧/秒选取 7,813 帧，其中 5,595 帧产生了有效面部特征。**7,813 是抽样处理的帧数，不是保存了 7,813 张图片。**最终 `NPZ` 保存的是按文本词片时间窗聚合的 61 维视觉特征，并保存抽样帧的时间戳和有效掩码；没有保存每张原始图片，也没有保存逐帧 61 维向量。24/100 段原方案完全没有检出人脸。100 段中自动规则 37 段 `PASS`、63 段 `REVIEW`，共 168/1932 个词待复核。`PASS` 只代表自动规则通过，不等于词边界或说话人身份已获人工证实。

## 1. 检查已上传的 Q1 特征

输入：`data/processed/q1/*.json` 与 `*.npz`，不需原视频或模型。命令：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_validate
```

输出：`results/q1_validation.json` 和 `results/q1_review_queue.csv`。检查样本数、数组形状、数值、掩码、窗口与记录的哈希。`status=PASS` 表示文件结构检查通过；逐样本 `REVIEW` 仍需人工审查。若缺文件或校验失败，请先检查是否完整拉取了 `data/processed/q1/`，不要直接修改结果文件凑数。

新版候选数据位于 `data/processed/q1_v2/` 时，加 `--variant q1_v2`；报告写入 `results/q1_v2/`，不会改动旧版。

## 2. 统计图：词对齐覆盖率与人脸检出率

输入：100 个 Q1 JSON，无需原视频。命令：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_quality_dashboard
```

输出：`results/q1_quality_proxy_metrics.csv`（逐视频）、`results/q1_quality_proxy_report.json`（汇总）、`results/q1_quality_proxy_dashboard.png` 和 `.svg`。图中词覆盖率是“自动判定有效的词 / 原文词”；人脸检出率是“有效面部抽样帧 / 全部抽样帧”。它们是**覆盖率代理指标**，不能代替人工词边界误差或正确说话人检验。

新版候选数据使用 `-m src.q1_quality_dashboard --variant q1_v2`，对应结果放在 `results/q1_v2/`。

## 3. 三模态沿时间展开的热图

输入：已有 Q1 JSON/NPZ，无需原视频。可固定随机种子、指定样本，或允许抽到 `REVIEW` 样本：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_timeline_demo --seed 23
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_timeline_demo --sample-id='-egA8-b7-3M$_$26'
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_timeline_demo --seed 23 --include-review
```

输出：`results/q1_demo/<样本ID>_timeline.png`、`.svg`、`last_demo.json`。三行依次是 768 维文字、70 维音频、61 维视觉特征，横轴为原视频秒数。灰色表示该时间没有有效词或该模态没有有效特征；颜色是**单段视频内每一维的标准化变化**，不能跨视频比较特征绝对大小。热图的色块不是每个 0.05 秒都重新提取一次特征，而是将按词得到的值铺在该词时间段上。

加 `--variant q1_v2` 可绘新版候选热图，输出到 `results/q1_v2/q1_demo/`。

## 4. 四层人工审查图：词、声音、人脸、原始画面

输入：对应的 Q1 JSON/NPZ、**原始 MP4**。本机运行示例：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_review_figure --sample-id='-egA8-b7-3M$_$26'
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_review_figure --sample-id='-571d8cVauQ$_$0' --times='0.8,2.0,3.2,4.3'
```

`--sample-id` 是精确视频 ID，省略时用第一条示例；`--times` 可指定**恰好四个**画面时间点（秒），省略时自动取全片约 16%、38%、62%、84% 的位置。命令输出 JSON，列出解码帧数、抽样帧数、有效人脸帧数和图片路径。图片与矢量版写到 `results/q1_review_figures/<样本ID>_review.png/.svg`。

读图顺序：上排蓝色词块为自动接受的词区间，橙色为低可信区间；第二排为原始音频每 10 毫秒计算的 RMS 音量；第三排每个点是一个抽样帧，绿点表示现有结果认为人脸有效，红点表示无有效人脸；下排是四张原始画面，竖虚线与画面时间相同。词与声音的对应关系要**听原声**确认；画面中有脸但第三排持续红色，说明现有检测漏检。下排画面标题使用最接近该时间的抽样帧状态，不能证明画中人物是说话人。此图只供审查，不会修改 Q1 特征。

加 `--variant q1_v2` 可查看新版，输出到 `results/q1_v2/q1_review_figures/`。例如：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_review_figure --variant q1_v2 --sample-id='-571d8cVauQ$_$0'
```

## 5. 人脸阈值和画面裁剪试验

输入：Q1 JSON 指向的原视频、`models/face_landmarker.task`、MediaPipe 和 PyAV。模型加载使用 `models/face_landmarker.task` **相对路径**，所以必须在 `工程` 目录执行。默认运行 6 条零人脸样本和 1 条正常对照样本，比较原阈值 0.5、较低阈值，以及保持原阈值但居中裁剪为 70% 或 50% 的画面：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_face_threshold_probe
```

默认输出 `results/q1_face_threshold_probe/threshold_probe.csv`，检出时另生成带绿色人脸框的四帧拼图 PNG。`detected_frames/sampled_frames` 是当前试验条件下的检出帧数/抽样帧数。绿色框必须人工看是否落在**目标人脸**，不能仅凭数量认定视觉特征可信。

指定样本、帧率和方案，例如：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_face_threshold_probe '--ids=-571d8cVauQ$_$0,-egA8-b7-3M$_$26' --fps 10 '--settings=original_0.5,center_crop_0.7,center_crop_0.5' --output=selected_probe.csv
```

`--ids` 是逗号分隔的精确 ID；由于 ID 以 `-` 开头，务必写成 `--ids=...` 并用单引号。`--settings` 可选 `original_0.5`、`relaxed_0.3`、`relaxed_0.2`、`center_crop_0.7`、`center_crop_0.5`；`--fps` 默认 10；`--output` 只能是输出目录内的 `.csv` 文件名；`--output-dir` 是 `results/` 下的一级目录名，默认 `q1_face_threshold_probe`；`--no-contact-sheets` 可关闭拼图生成。本次全体 24 条零人脸样本的裁剪结果在 `results/q1_face_threshold_probe/all_zero_crop.csv`。70% 裁剪使 18/24 段出现人脸检出，50% 裁剪使 19/24 段出现人脸检出（1283/1637 个抽样帧）。对抽查的 6 段，单纯降低阈值到 0.3 或 0.2 均仍是零。个别视频可能是电视画面、多人或远景，需逐段核对。试验**没有重算或覆盖** `data/processed/q1/`；检出数还未经过原特征提取程序的人脸身份跳变与 61 维向量有效性检查。

如需重算全部 24 条现有零人脸视频，在 PowerShell 中运行：

```powershell
$q1Ids = Get-ChildItem 'data/processed/q1' -Filter '*.json' | Where-Object { (Get-Content -LiteralPath $_.FullName -Raw | ConvertFrom-Json).video_face_frames -eq 0 } | ForEach-Object { $_.BaseName }
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_face_threshold_probe ('--ids=' + ($q1Ids -join ',')) '--settings=center_crop_0.7,center_crop_0.5' --output=all_zero_crop.csv --no-contact-sheets
```

## 6. 准备人工复核表

先运行第 1 节得到最新 `q1_review_queue.csv`，再执行：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_prepare_manual_review
```

输出：`results/q1_manual_review_sheet.csv`。每段一行整段检查，再按异常点追加行。结合第 4 节的图和原视频填写“人工判定”“修正起止秒”“审查者”“备注”；填写规则见 `results/q1_manual_review_instructions.md`。该程序重新生成表格会**覆盖旧表**；若已有人填写，先备份人工填写版本，再运行。

## 7. 从原始附件重新提取 Q1（完整环境）

需要题目原始附件、100 条标签表、BERT、Wav2Vec2、MediaPipe 面部模型和完整 Python 依赖；仓库克隆本身不具备这些条件。重跑前后按 `AGENTS.md` 核验原始附件：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.tools.verify_raw_integrity
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_extract --limit 3 --fps 10
& ..\训练demo\.venv\Scripts\python.exe -m src.tools.verify_raw_integrity
```

`--limit 3` 只处理标签表前 3 条；全量使用 `--fps 10 --resume`；`--ids` 可指定逗号分隔 ID，`--fps 0` 表示处理所有视频帧。输出写入 `data/processed/q1/` 和 `results/q1_feature_inventory.csv`。**重跑可能覆盖同名 Q1 派生结果**；先明确自己要重算哪些样本，保存旧结果并记下配置。重新提取后再运行第 1、2 节，并对第 4 节抽样人工复核。第一问特征仅用于第一问，不作为第二、三问的训练输入。

## 8. Q1 新版视觉提取、指标对比与人工审查（2026-09-23）

新版候选只重算**低人脸覆盖率**样本的视觉提取；文字、音频和词时间沿用已校验旧版，使人脸改动可直接比较。处理后仍保留旧版 `data/processed/q1/`，新版写 `data/processed/q1_v2/`。提取程序读取原视频，因此必须先按第 7 节检查原始附件完整性；它还需要 PyAV、MediaPipe 模型，以及导入 `src.q1_extract` 所需的完整环境。默认阈值为旧版人脸帧率低于 50%；对这类视频分别试居中裁剪 70%、50%，只在有效面部帧数**严格多于旧版**时选用，平局优先 70%。使用裁剪的视频一律保留 `REVIEW` 状态，等待确认识别的是目标说话人。

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.tools.verify_raw_integrity
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_visual_refine
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_validate --variant q1_v2
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_quality_dashboard --variant q1_v2
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_compare_versions
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_prepare_manual_review --variant q1_v2
& ..\训练demo\.venv\Scripts\python.exe -m src.tools.verify_raw_integrity
```

`src.q1_visual_refine` 可用 `--limit N` 或 `--ids='ID1,ID2'` 小范围试跑；正式比较必须重新运行**全 100 段**，否则核验会报告样本不全。该程序会覆盖**同名新版候选**文件和 `results/q1_v2/q1_feature_inventory.csv`，不会覆盖旧版。`src.q1_compare_versions` 无需原视频：逐项验证文字、音频、词时间等数组与旧版一致，并输出 `results/q1_v2/q1_version_comparison.csv/.json` 和按优先级排列的 `q1_face_review_priority.csv`。`src.q1_prepare_manual_review --variant q1_v2` 输出可填写的 `results/q1_v2/q1_manual_review_sheet.csv`；重复运行会覆盖人工填写，请先另存人工版。

本轮实测：100/100 新版文件结构通过；27 段选择裁剪，5 段仍零人脸；有效人脸帧从 5595/7813 增至 7110/7813，词级视觉有效位置从 1562 增至 2008。新方案相比旧版逐帧新增 1521 个检出，同时失去 6 个旧版检出，净增 1515。词对齐覆盖率仍是 0.913，因为这轮没有改文字、音频和词边界。这些全是**自动覆盖率**，不是说话人身份准确率或时间误差；正式人工审查顺序见 `results/q1_v2/人工审查指引.md`。

27 段采用裁剪的视频已经生成带绿色检测框的四帧拼图，目录是 `results/q1_v2_face_contact_sheets/`，文件名为 `<样本ID>_<visual_mode>.png`。若要重生成其中一段，可从 `q1_face_review_priority.csv` 查到 `visual_mode`，例如：

```powershell
& ..\训练demo\.venv\Scripts\python.exe -m src.q1_face_threshold_probe '--ids=-571d8cVauQ$_$0' '--settings=center_crop_0.7' --output-dir=q1_v2_face_contact_sheets --output=single_probe.csv
```

检测框拼图用于核对是否是**正在说话的目标人物**；四层审查图用于核对词、声音和原始画面是否在同一时间。拼图中的纯检测计数可能略高于最终新版有效帧数，因为最终的 61 维提取还检查人脸几何、身份跳变和数值有效性。

本次 32 段优先片段的四层审查图已全部生成在 `results/q1_v2/q1_review_figures/`，每段有 PNG 和 SVG。若原视频仍在本机，也可按优先级表一次性重画：

这些图包含从题目原视频截出的可识别人物画面，**只留在本机**，本次 Git 更新不上传；共享仓库的队友需备好授权原视频后按下面命令重画。

```powershell
$ids = Import-Csv 'results/q1_v2/q1_face_review_priority.csv' | ForEach-Object sample_id
foreach ($id in $ids) { & ..\训练demo\.venv\Scripts\python.exe -m src.q1_review_figure --variant q1_v2 ('--sample-id=' + $id) }
```

## 常见问题

- 提示找不到原视频：检查 JSON `video_path` 是否是**当前电脑**可用的路径。Git 中的绝对路径只是原开发机记录；仓库没有原视频。
- 提示找不到模型：检查 `models/face_landmarker.task` 与完整提取所需的 BERT/Wav2Vec2 权重是否已单独备好。
- PowerShell 把 `$` 变成变量：整个样本 ID 或 `--ids=...` 参数使用单引号。
- 只有统计图，没有原始帧图：第 4、5 节需要原 MP4；已上传的 61 维特征不能还原画面。
- `PASS` 视频也可能有词位移或认错人：自动规则无法验证这两点，按第 4、6 节人工检查。
