# E题 T0 独立复验回执（2026-09-23，本轮）

**结论：FAIL，暂不签署 T0/M1 PASS；不启动 T1。** 数据读取与主要统计已独立复现，但审计产物哈希和失败注入证据不自洽，尚不能作为可靠的 T0 验收合同。

## 独立实跑

工作目录：`D:\第二十三届中国研究生数学建模竞赛 - 中文题目\中文题目\E题\工程`。实际解释器：`D:\第二十三届中国研究生数学建模竞赛 - 中文题目\中文题目\E题\训练demo\.venv\Scripts\python.exe`；下表 `python` 指该解释器的绝对路径。只读输入遵守 `E题/AGENTS.md`，未改动原始附件及 demo。

| 命令 | 退出码 | 独立观察 |
|---|---:|---|
| `python -m src.tools.verify_raw_integrity`（主审计前） | 0 | 245 个压缩包内文件与解压文件及清单一致；原始 zip SHA-256 为 `F895003ABEB3E972E69EF9C457032720CBCB8ECD3D2261A0E30BC15667604ACB`；证据 `results/codex_review/raw_integrity_20260923T084123543307Z.json` |
| `python -m src.audit --config configs/base.json` | 0 | 11 步均报告 PASS；内部耗时 109.24 秒，证据为本轮 `results/audit_run_report.json`、`STATUS.md` |
| `python -m src.tools.verify_raw_integrity`（主审计后） | 0 | 再次 245 项通过；证据 `results/codex_review/raw_integrity_20260923T084409951138Z.json` |
| PowerShell `Get-FileHash results/data_audit.json -Algorithm SHA256` 并与 `audit_run_report.json` 比较 | 0 | **不一致**，见下 |
| 读取失败注入证据中 `STATUS.md` 和 `results/audit_run_report.json` | 0 | 一个仍称 PASS，一个写 FAIL，见下 |

独立复现的主要数据事实：附件二 train/valid/test 为 3395/728/727；附件三 30 文件、附件四 20 文件均被统一读取器读出；清单 245 行；附件一与附件二精确相同样本 ID 为 18 个（11/0/7），源视频 ID 为 24 个；BERT 重分词 4870/4870 全等、向量重构最大绝对误差约 `5.966e-4`、超过 `1e-3` 的样本 0 个；附件一视频容器帧数差异 90/100，OpenCV/PyAV 解码帧数一致 100/100。以上仅表示这些检查在本机复现，不等于验收产物可靠。

## 必须返工

1. **P0：审计文件哈希错误。** 本轮 `audit_run_report.json` 与 `STATUS.md` 均报告 `results/data_audit.json` 的 SHA-256 为 `9644bbe178770536405d82000261df29df082954325cded2ec09e39a87d4746b`；最终磁盘文件实测为 `F2E78C9FCBC6F230DD7BE7E57EE780825C087AD655C88EC935C85A3AC400C612`。其余列入回执的产物哈希与磁盘一致。原因见 `src/audit.py`：先写 `data_audit.json`、计算哈希，随后把哈希表写回同一文件（约第 997–1015 行）。最终文件不能再沿用先前哈希。应避免文件自哈希循环，将最终文件哈希只写到外部回执/状态，或从本文件哈希表排除自身；重跑后逐项校验报告与磁盘一致。
2. **P0：失败注入证据与回执矛盾。** `results/t0_before_fix/fail_injection_evidence/results/audit_run_report.json` 是 `FAIL`，失败步骤 `unified_reader`；同目录 `STATUS.md` 中没有 `BLOCKED`，反而有“全部子检查 PASS”。DeepSeek 回执声称副本 STATUS 标为 BLOCKED，与交付文件不符。`base.testroot.json` 将 `project_root` 指向 scratch 根、`engine_dir` 设为 `工程`，而 `results_dir` 指向 scratch 根下的 `results`；应核对实际写入的 `scratch/工程/STATUS.md`，并让失败注入测试读取真实目标、断言其为 BLOCKED 且无 PASS 结论。保存正确的命令、退出码、目标路径和文件哈希。
3. **P1：产物写失败仍可能退出 0。** `src/audit.py` 的 `write_if_possible()` 捕获 `OSError` 后只打印 WARN；`finalize()` 对写 `data_audit.json`、`interface_contract.json`、`STATUS.md`、`audit_run_report.json` 的返回值均未检查，最后仍打印“Updated STATUS.md successfully”并返回 0。关键产物写入失败必须非零退出；建议对这些文件使用必须成功的写入函数，并在最终成功前确认文件存在、能解析、哈希与外部回执一致。

## 未作为失败点的边界

词级真实时间映射尚无结果，Q1/Q3 应继续标 `unresolved`；附件一视频帧数差异根因及音频完整性仍未知；说话人独立性未知；50 MB 为预算，非实测打包；T1 的 FP32 转换、有限值检查和模拟缺失未执行。这些结论在 T0 范围内按未决项记录即可。

返工只改 `E题/工程` 中的代码、结果与状态；保留本回执和原始输入，不启动 T1。修完后先做失败注入与实际哈希核对，再重跑主审计及前后完整性检查，交回独立复验。
