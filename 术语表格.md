# E 题第二问术语与符号表

> 与《题目分析报告.md》配套；只收录跨段复用的全局符号。维度没有被题面定义为物理量的，统一记“无量纲特征值”。

| 中文术语 | English | 符号 | 定义 | 单位 | 首次出现 | 禁止混用的近义词或解释边界 |
|---|---|---|---|---|---|---|
| 样本 | sample/clip | $i$ | 一个视频片段及其三模态特征和片段标签 | 个 | §2 | 不等于源视频 ID；同一视频可有多个片段 |
| 模态 | modality | $m\in\{T,A,V\}$ | 文本 T、语音 A、视觉 V | 无量纲 | §4 | 不把“视觉”叫原始图片；此处是 35 维特征 |
| 对齐位置 | aligned token step | $t\in\{1,\ldots,50\}$ | 对齐文件的序列位置 | token 位 | §2 | 不等于 1/50 视频秒数或真实词时间戳 |
| 有效长度 | valid sequence length | $L_i$ | `text_bert` 注意力掩码中非补齐位置数，含特殊 token | token 位 | §2 | 不等于固定 50，也不等于视频时长 |
| 内容长度 | content length | $K_i$ | $L_i-2$，去掉 `[CLS]` 和 `[SEP]` 后的有效内容位数 | token 位 | §4 | 是遮蔽长度分母，不是视频秒数 |
| 文本有效位 | attention-valid step | $V_{it}$ | `attention_mask` 为 1 的位置 | 0/1 | §4 | 不等于真实模态可观测位 |
| 内容位 | content-token step | $C_{it}$ | 有效且不为 `[CLS]`/`[SEP]` 的位置 | 0/1 | §4 | 不把特殊 token 当真实词 |
| 原始模态向量 | source feature vector | $\mathbf x_{itm}$ | 附件二给定模态在位置 $t$ 的特征行 | 无量纲特征值 | §4 | 不猜测 74/35 维逐维物理语义 |
| 原始可观测位 | observed-feature mask | $O_{itm}$ | 内容位内可供模型读取的原始特征标记；音视频全零位暂记不可观测 | 0/1 | §4 | “不可观测”只是数据处理标记，不证明遮挡原因 |
| 模拟遮蔽位 | synthetic missing mask | $S_{itm}$ | 训练或固定验证面板人为设为缺失的连续位置 | 0/1 | §4 | 不等于原始零向量或 padding |
| 最终可用位 | available-feature mask | $R_{itm}$ | $O_{itm}(1-S_{itm})$ | 0/1 | §4 | 不与 `attention_mask` 混为同一掩码 |
| 模态隐向量 | projected modality vector | $\mathbf h_{itm}$ | 模态投影后 128 维表示 | 无量纲 | §5 | 不是原始 768/74/35 维向量 |
| 门控权重 | fusion gate weight | $\alpha_{itm}$ | 当前位置各可观测模态融合的非负归一化权重 | 无量纲 | §5 | 不直接解释为因果贡献或第三问结论 |
| 融合向量 | fused representation | $\mathbf z_{it}$ | 当前位置三模态门控融合结果 | 无量纲 | §5 | 不等于最终片段预测 |
| 情感极性 | sentiment polarity | $c_i,\hat c_i$ | 真实/预测的负、中、正三类 | 类别 | §3 | 不把“中性”直接设为某个随意强度阈值 |
| 分类 logits | class logits | $\mathbf q_i$ | 分类头未归一化的三维输出，交叉熵的输入 | 无量纲 | §5 | 不把离散预测类别送进交叉熵 |
| 分类概率 | class probability | $\mathbf p_i$ | $\operatorname{softmax}(\mathbf q_i)$ 得到的三类概率 | 比例 | §5 | 不与 logits 或硬类别混写 |
| 情感强度 | sentiment intensity | $y_i,\hat y_i$ | 真实/预测的连续情感分值，范围 $[-3,3]$ | 情感分值 | §3 | 不把极性类别编码当回归标签 |
| 类别权重 | class weight | $w_c$ | 仅由 train 类频次得到的交叉熵权重 | 无量纲 | §5 | 不由 valid/test/附件三估计 |
| 回归损失权重 | regression loss weight | $\lambda_r$ | Huber 损失在联合目标中的系数，本轮固定为 1 | 无量纲 | §5 | 是预设配置，不是数据事实或已验证最优值 |
| 缺失视图权重 | masked-view loss weight | $\lambda_m$ | 遮蔽视图监督损失的系数，本轮固定为 1 | 无量纲 | §5 | 不代表真实缺失比例 |
| 准确率 | accuracy | $\mathrm{Acc}$ | 三类极性预测正确数除以样本数 | 比例 | §1 | 与 macro-F1 分开报告 |
| 宏平均 F1 | macro-averaged F1 | $F_{1,\mathrm{macro}}$ | 三类 F1 的算术平均 | 比例 | §2 | 不与 weighted-F1 混写 |
| 平均绝对误差 | mean absolute error | $\mathrm{MAE}$ | $|\hat y_i-y_i|$ 的样本均值 | 情感分值 | §1 | 越小越好 |
| 皮尔逊相关系数 | Pearson correlation | $r$ | 预测与真实强度的线性相关 | 无量纲 | §1 | 不代表 MAE 小；常值预测时需定义不可计算处理 |
| 缺失长度 | missing span length | $\ell$ | 一个连续区间包含的有效内容 token 位数 | token 位 | §4 | 不得写成秒；可同时报告 $\ell$ 占内容长度比例 |
| 有效模态位数 | available modality count | $n_{im}$ | 样本 $i$ 的模态 $m$ 在全部内容位上的可用位数 | token 位 | §5 | 为零时使用全零汇聚和存在标志，不除以零 |
| 模型选择分数 | validation selection score | $J$ | 固定 clean/缺失验证面板的 MAE 与 macro-F1 组合，越低越好 | 无量纲 | §5 | 是预设选型准则，不是官方竞赛指标 |
| 完整与遮蔽视图 | clean/masked view | $x,\tilde x$ | 同一样本的原始可用特征视图与连续局部遮蔽视图 | 无量纲特征值 | §5 | 不把遮蔽视图当新增独立标签样本 |
