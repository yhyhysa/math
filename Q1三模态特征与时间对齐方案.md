# E题第一问：三模态特征与时间对齐方案

**状态**：100 条附件一视频已完成特征提取，结构校验通过；37 条通过自动质控，63 条需人工复核。输入是附件一 100 条视频和 `label-100.xlsx` 的给定英文转写。标签仅供核对和样本描述；不参与特征提取。

**问题边界**：本文件及 `data/processed/q1/` 的特征只服务于**问题一**的特征说明、时间对齐、核验与提交。问题二、三按题面使用附件二的训练/验证数据及规定的专项附件；**不读取问题一自提的特征作为训练、验证、调参或模型输入**。附件一与附件二有重叠样本 ID，也不改变这条边界。

## 1. 输出目标

以**给定转写的 BERT 子词 token**为索引，保留每个子词所对应的原词、真实音频时间区间和视频时间区间。对每个 token 产出：文本 `768` 维、音频 `70` 维、视觉 `61` 维特征，并分别记录有效性掩码。`text_bert` 为 `input_ids / attention_mask / token_type_ids` 三行。特征维度 70/61 是本方案自定义；附件二的 74/35 维由题目预计算，逐维含义未知，不能混称或直接逐维比较。

**问题一以完整可变长序列为主，题面没有规定 50 个 token 或最多 50 个 token。**实测附件一 100 条给定文本按本机固定的 `bert-base-uncased` 分词，包含 `[CLS]/[SEP]` 后长度为 7–84，6 条超过 50。当前工程额外保存长度 50 的重叠窗口，仅作内部核验/读取便利，不是题目要求，也不取代完整序列；提交时可按体积和可复现性需要决定是否保留。题面中“50 是最大序列位置数，有效位置可能小于 50，剩余位置填充”只说明**附件二预处理文件** `aligned_50.pkl` 和 `unaligned_50.pkl` 的格式。问题一自提特征不进入问题二、三模型。

## 2. 特征定义

| 模态 | 特征组成 | 帧/序列处理 | 缺失处理 |
|---|---|---|---|
| 文本 `L×768` | 给定英文转写按固定 revision 的 `bert-base-uncased` Fast tokenizer 编码，取冻结 BERT 最后一层 token 向量。保存原词到子词的映射；`[CLS]/[SEP]/[PAD]` 不对应音视频。 | 一次处理整个句子并完整保留全部 token；当前可选的 50 位置窗口须记录重叠和覆盖情况，不能截断正文。 | 文本原句缺失或分词失败时记录失败；不能用 ASR 输出悄悄替换给定转写。 |
| 音频帧 `T_a×70` | **25** 个 openSMILE eGeMAPSv02 低层描述符 + **20** 个 MFCC 系数 + **20** 个 MFCC 一阶差分 + **5** 个附加量（RMS、过零率、谱质心、谱带宽、谱滚降）= 70。逐维名称、量纲、配置随文件保存。 | 视频音轨解码为单声道 16 kHz；统一以 10 ms 步长、约 20–25 ms 窗长计算，各来源重采样到共同帧时间轴，保存每帧时间区间。MFCC 差分在窗口边缘按明确规则计算。 | 无声帧、无基频帧和解码缺口分别记录；基频不可用不等同于整个音频缺失。全部原始数值需有限值检查。 |
| 视觉帧 `T_v×61` | MediaPipe Face Landmarker 的 **52** 个 blendshape 系数 + 从面部变换矩阵计算的 **3** 个头部角（yaw/pitch/roll）+ **6** 个按双眼间距归一的几何比值（左右眼开合、嘴开合、嘴宽、左右眉眼距离）= 61。几何点位索引和坐标约定需固定在配置中。 | 优先处理附件一全部实际解码帧（T0 实测总计 23,241 帧），使用真实 PTS；若速度不足，按 PTS 均匀降到约 10 fps，并报告覆盖率。对人物进行连续跟踪，避免帧间切换到背景人脸。 | 无脸、追踪丢失或质量差时 `vision_valid=false`；不把补零或重复上一帧当作真实表情。 |

70/61 维是**可实施的初始特征合同**，不是题目预先规定的维数。先用 3 条不同长度、不同画质的视频跑通并检查数值范围；若工具输出的维数或可用性与上表不符，应修改配置、逐维说明和结果文件，而不是硬裁剪到目标数。

## 3. 建立真正的时间轴

1. **解码与时钟**：音频重采样时保留原音轨起点与时长；视频帧以解码 PTS 为准。T0 已发现 90/100 条容器标称帧数与实际解码数不一致，因此不得按“帧号 ÷ 标称 fps”推时间。
2. **词级强制对齐**：将 `label-100.xlsx` 的给定转写作为已知文本，用语音与文本的强制对齐器求每个词的 `[start,end]` 秒数。Whisper 可作为转写质量抽查，但不是主文本来源；只运行 Whisper 本身不能获得可靠的逐词时间。保留对齐模型、分数、未对齐词和失败原因。
3. **词到子词**：用 Fast tokenizer 的词/字符映射，将一词拆成的多个 BERT 子词都关联到这个词的时间区间。词边界与标点归属规则固定；特殊 token 只保留文本特征，其音视频掩码为 false。
4. **时间聚合**：对词区间 $I_j$ 和音频帧区间 $A_k$、视频帧支撑区间 $V_l$ 求交集时长。音频或视觉词向量为 $\sum_k |I_j\cap A_k|f_k / \sum_k |I_j\cap A_k|$（视觉同理），只纳入有效帧。视频帧支撑区间由相邻真实 PTS 的中点界定。对齐失败或区间内无有效帧时设相应 `valid=false`；最近邻只允许在预设的短时间容差内补配，并保留 `imputed=true` 标记。
5. **质量复核**：检查词区间递增且处于实际音轨/视频覆盖范围，音视频有效覆盖率、丢脸率、未对齐词比例和可选窗口的正文覆盖率。抽查长句、短句、两条音频时长异常样本，以及对齐失败样本；人工回看原帧和原音频。

## 4. 交付文件与验收

每条视频至少保存 `sample_id`、输入文件 SHA-256、给定转写、完整 token 列表、token→词索引、词起止时间、原始帧 PTS、`text/audio_aligned/vision_aligned`、三种有效性掩码、补配标记、工具配置和失败原因。建议逐样本压缩存为 `工程/data/processed/q1/<sample_id>.npz`，可读元数据存为同名 JSON；`工程/results/q1_feature_inventory.csv` 汇总 100 条的维数、长度、覆盖率和状态。全部派生数据只写入 `工程/`，原始视频与 `label-100.xlsx` 只读。

**验收状态**：100/100 条有特征文件，维数、词/token 映射和文件完整性已通过程序核对；`results/q1_validation.json` 记录结果，`results/q1_review_queue.csv` 列出待人工核查项。词时间准确性和人脸身份连续性尚未完成全面人工确认，63 条 `REVIEW` 不能写成已准确对齐。

## 5. 方法来源

- [WhisperX 官方仓库](https://github.com/m-bain/whisperX)：语音与给定文本强制对齐可得词级时间；具体模型和离线依赖须在实施时固定。
- [openSMILE Python 官方文档](https://audeering.github.io/opensmile-python/)：eGeMAPSv02 低层描述符为 25 维；70 维是本方案将多个来源组合后的维数。
- [MediaPipe Face Landmarker 官方文档](https://ai.google.dev/edge/api/mediapipe/python/mp/tasks/vision/FaceLandmarkerOptions)及[52 个 blendshape 名称](https://ai.google.dev/edge/api/mediapipe/python/mp/tasks/vision/drawing_styles/face_landmarker/Blendshapes)：blendshape 和面部变换矩阵需要显式开启输出。
- [Hugging Face tokenizer 文档](https://huggingface.co/docs/transformers/main_classes/tokenizer)：Fast tokenizer 可提供原文字符/词与子词位置的映射。
