# E 题工程协作规则

1. `data/raw/`、原始题面、Excel 的所有工作表及标签、原始视频和题目 train/valid/test 划分只读。不得为使结果通过而改写、删减、替换或重命名原始文件。用户确认的数据版本变更须另存证据。
2. 新代码写入 `src/`；派生数据写入 `data/processed/`，报告和人工修正副本写入 `results/`。不要覆盖原始输入或伪造既有审计结果。
3. 处理原始附件前后运行 `python -m src.tools.verify_raw_integrity`。若克隆仓库中没有原始附件，应明确记录“未运行原始完整性核验”，不得宣称通过。既有 E题数据.zip 的基准 SHA-256 为 `F895003ABEB3E972E69EF9C457032720CBCB8ECD3D2261A0E30BC15667604ACB`。
4. 问题一的 `data/processed/q1/` 特征只用于第一问分析、核验与展示；问题二、三按赛题使用规定附件的数据训练、验证和专项推理，不将问题一特征并入训练或调参。
5. 提交前检查 `.gitignore`；原始数据、模型权重、依赖包、虚拟环境和临时日志不进入 Git。自动质控 `PASS` 不是人工对齐准确性证明。
